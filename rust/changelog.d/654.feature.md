Expose public JWT generation: add standalone `generate_jwt(JwtOptions)` function and make `WalletAuth::generate_jwt` public, enabling users to generate JWTs independently for use cases like x402 facilitator API authentication.
