/*
 * Coinbase Developer Platform APIs
 * The Coinbase Developer Platform APIs - leading the world's transition onchain.
 *
 * The version of the OpenAPI document: 2.0.0
 * Contact: cdp@coinbase.com
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package com.coinbase.cdp.openapi.model;

import java.net.URLEncoder;
import java.nio.charset.StandardCharsets;
import java.util.StringJoiner;
import java.util.Objects;
import java.util.Map;
import java.util.HashMap;
import com.coinbase.cdp.openapi.model.PaymentSourceCoinbase;
import com.coinbase.cdp.openapi.model.PaymentSourceNetwork;
import com.coinbase.cdp.openapi.model.PaymentSourceWallet;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonTypeName;
import com.fasterxml.jackson.annotation.JsonValue;
import java.util.Arrays;
import com.fasterxml.jackson.annotation.JsonPropertyOrder;

import com.fasterxml.jackson.core.type.TypeReference;

import java.io.IOException;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;

import com.fasterxml.jackson.core.JsonGenerator;
import com.fasterxml.jackson.core.JsonParser;
import com.fasterxml.jackson.core.JsonProcessingException;
import com.fasterxml.jackson.core.JsonToken;
import com.fasterxml.jackson.databind.DeserializationContext;
import com.fasterxml.jackson.databind.JsonMappingException;
import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.MapperFeature;
import com.fasterxml.jackson.databind.SerializerProvider;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;
import com.fasterxml.jackson.databind.deser.std.StdDeserializer;
import com.fasterxml.jackson.databind.ser.std.StdSerializer;
import com.coinbase.cdp.openapi.JSON;

@jakarta.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", comments = "Generator version: 7.11.0")
@JsonDeserialize(using = PaymentSessionSource.PaymentSessionSourceDeserializer.class)
@JsonSerialize(using = PaymentSessionSource.PaymentSessionSourceSerializer.class)
public class PaymentSessionSource extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(PaymentSessionSource.class.getName());

    public static class PaymentSessionSourceSerializer extends StdSerializer<PaymentSessionSource> {
        public PaymentSessionSourceSerializer(Class<PaymentSessionSource> t) {
            super(t);
        }

        public PaymentSessionSourceSerializer() {
            this(null);
        }

        @Override
        public void serialize(PaymentSessionSource value, JsonGenerator jgen, SerializerProvider provider) throws IOException, JsonProcessingException {
            jgen.writeObject(value.getActualInstance());
        }
    }

    public static class PaymentSessionSourceDeserializer extends StdDeserializer<PaymentSessionSource> {
        public PaymentSessionSourceDeserializer() {
            this(PaymentSessionSource.class);
        }

        public PaymentSessionSourceDeserializer(Class<?> vc) {
            super(vc);
        }

        @Override
        public PaymentSessionSource deserialize(JsonParser jp, DeserializationContext ctxt) throws IOException, JsonProcessingException {
            JsonNode tree = jp.readValueAsTree();
            Object deserialized = null;
            boolean typeCoercion = ctxt.isEnabled(MapperFeature.ALLOW_COERCION_OF_SCALARS);
            int match = 0;
            JsonToken token = tree.traverse(jp.getCodec()).nextToken();
            // deserialize PaymentSourceCoinbase
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (PaymentSourceCoinbase.class.equals(Integer.class) || PaymentSourceCoinbase.class.equals(Long.class) || PaymentSourceCoinbase.class.equals(Float.class) || PaymentSourceCoinbase.class.equals(Double.class) || PaymentSourceCoinbase.class.equals(Boolean.class) || PaymentSourceCoinbase.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((PaymentSourceCoinbase.class.equals(Integer.class) || PaymentSourceCoinbase.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((PaymentSourceCoinbase.class.equals(Float.class) || PaymentSourceCoinbase.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (PaymentSourceCoinbase.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (PaymentSourceCoinbase.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(PaymentSourceCoinbase.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'PaymentSourceCoinbase'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'PaymentSourceCoinbase'", e);
            }

            // deserialize PaymentSourceWallet
            try {
                boolean attemptParsing = true;
                // ensure that we respect type coercion as set on the client ObjectMapper
                if (PaymentSourceWallet.class.equals(Integer.class) || PaymentSourceWallet.class.equals(Long.class) || PaymentSourceWallet.class.equals(Float.class) || PaymentSourceWallet.class.equals(Double.class) || PaymentSourceWallet.class.equals(Boolean.class) || PaymentSourceWallet.class.equals(String.class)) {
                    attemptParsing = typeCoercion;
                    if (!attemptParsing) {
                        attemptParsing |= ((PaymentSourceWallet.class.equals(Integer.class) || PaymentSourceWallet.class.equals(Long.class)) && token == JsonToken.VALUE_NUMBER_INT);
                        attemptParsing |= ((PaymentSourceWallet.class.equals(Float.class) || PaymentSourceWallet.class.equals(Double.class)) && token == JsonToken.VALUE_NUMBER_FLOAT);
                        attemptParsing |= (PaymentSourceWallet.class.equals(Boolean.class) && (token == JsonToken.VALUE_FALSE || token == JsonToken.VALUE_TRUE));
                        attemptParsing |= (PaymentSourceWallet.class.equals(String.class) && token == JsonToken.VALUE_STRING);
                    }
                }
                if (attemptParsing) {
                    deserialized = tree.traverse(jp.getCodec()).readValueAs(PaymentSourceWallet.class);
                    // TODO: there is no validation against JSON schema constraints
                    // (min, max, enum, pattern...), this does not perform a strict JSON
                    // validation, which means the 'match' count may be higher than it should be.
                    match++;
                    log.log(Level.FINER, "Input data matches schema 'PaymentSourceWallet'");
                }
            } catch (Exception e) {
                // deserialization failed, continue
                log.log(Level.FINER, "Input data does not match schema 'PaymentSourceWallet'", e);
            }

            if (match == 1) {
                PaymentSessionSource ret = new PaymentSessionSource();
                ret.setActualInstance(deserialized);
                return ret;
            }
            throw new IOException(String.format("Failed deserialization for PaymentSessionSource: %d classes match result, expected 1", match));
        }

        /**
         * Handle deserialization of the 'null' value.
         */
        @Override
        public PaymentSessionSource getNullValue(DeserializationContext ctxt) throws JsonMappingException {
            throw new JsonMappingException(ctxt.getParser(), "PaymentSessionSource cannot be null");
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, Class<?>> schemas = new HashMap<>();

    public PaymentSessionSource() {
        super("oneOf", Boolean.FALSE);
    }

    public PaymentSessionSource(PaymentSourceCoinbase o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public PaymentSessionSource(PaymentSourceWallet o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("PaymentSourceCoinbase", PaymentSourceCoinbase.class);
        schemas.put("PaymentSourceWallet", PaymentSourceWallet.class);
        JSON.registerDescendants(PaymentSessionSource.class, Collections.unmodifiableMap(schemas));
    }

    @Override
    public Map<String, Class<?>> getSchemas() {
        return PaymentSessionSource.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * PaymentSourceCoinbase, PaymentSourceWallet
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (JSON.isInstanceOf(PaymentSourceCoinbase.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        if (JSON.isInstanceOf(PaymentSourceWallet.class, instance, new HashSet<Class<?>>())) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be PaymentSourceCoinbase, PaymentSourceWallet");
    }

    /**
     * Get the actual instance, which can be the following:
     * PaymentSourceCoinbase, PaymentSourceWallet
     *
     * @return The actual instance (PaymentSourceCoinbase, PaymentSourceWallet)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `PaymentSourceCoinbase`. If the actual instance is not `PaymentSourceCoinbase`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `PaymentSourceCoinbase`
     * @throws ClassCastException if the instance is not `PaymentSourceCoinbase`
     */
    public PaymentSourceCoinbase getPaymentSourceCoinbase() throws ClassCastException {
        return (PaymentSourceCoinbase)super.getActualInstance();
    }

    /**
     * Get the actual instance of `PaymentSourceWallet`. If the actual instance is not `PaymentSourceWallet`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `PaymentSourceWallet`
     * @throws ClassCastException if the instance is not `PaymentSourceWallet`
     */
    public PaymentSourceWallet getPaymentSourceWallet() throws ClassCastException {
        return (PaymentSourceWallet)super.getActualInstance();
    }



  /**
   * Convert the instance into URL query string.
   *
   * @return URL query string
   */
  public String toUrlQueryString() {
    return toUrlQueryString(null);
  }

  /**
   * Convert the instance into URL query string.
   *
   * @param prefix prefix of the query string
   * @return URL query string
   */
  public String toUrlQueryString(String prefix) {
    String suffix = "";
    String containerSuffix = "";
    String containerPrefix = "";
    if (prefix == null) {
      // style=form, explode=true, e.g. /pet?name=cat&type=manx
      prefix = "";
    } else {
      // deepObject style e.g. /pet?id[name]=cat&id[type]=manx
      prefix = prefix + "[";
      suffix = "]";
      containerSuffix = "]";
      containerPrefix = "[";
    }

    StringJoiner joiner = new StringJoiner("&");

    if (getActualInstance() instanceof PaymentSourceWallet) {
        if (getActualInstance() != null) {
          joiner.add(((PaymentSourceWallet)getActualInstance()).toUrlQueryString(prefix + "one_of_0" + suffix));
        }
        return joiner.toString();
    }
    if (getActualInstance() instanceof PaymentSourceCoinbase) {
        if (getActualInstance() != null) {
          joiner.add(((PaymentSourceCoinbase)getActualInstance()).toUrlQueryString(prefix + "one_of_1" + suffix));
        }
        return joiner.toString();
    }
    return null;
  }

}

