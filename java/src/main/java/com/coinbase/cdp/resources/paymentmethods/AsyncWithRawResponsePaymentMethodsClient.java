/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.resources.paymentmethods;

import com.coinbase.cdp.core.CdpClientApiException;
import com.coinbase.cdp.core.CdpClientException;
import com.coinbase.cdp.core.CdpClientHttpResponse;
import com.coinbase.cdp.core.ClientOptions;
import com.coinbase.cdp.core.EndpointMetadata;
import com.coinbase.cdp.core.ObjectMappers;
import com.coinbase.cdp.core.QueryStringMapper;
import com.coinbase.cdp.core.RequestOptions;
import com.coinbase.cdp.errors.BadRequestError;
import com.coinbase.cdp.errors.InternalServerError;
import com.coinbase.cdp.errors.NotFoundError;
import com.coinbase.cdp.errors.UnauthorizedError;
import com.coinbase.cdp.resources.paymentmethods.requests.GetPaymentMethodRequest;
import com.coinbase.cdp.resources.paymentmethods.requests.ListPaymentMethodsRequest;
import com.coinbase.cdp.resources.paymentmethods.types.ListPaymentMethodsResponse;
import com.coinbase.cdp.types.Error;
import com.coinbase.cdp.types.PaymentMethodId;
import com.coinbase.cdp.types.PaymentMethodsPaymentMethod;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.Override;
import java.lang.String;
import java.util.Collections;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncWithRawResponsePaymentMethodsClient {
  protected final ClientOptions clientOptions;

  AsyncWithRawResponsePaymentMethodsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * List payment methods linked to your entity. Payment methods represent external financial instruments that can be used as a target for transfers. The list will not include disabled or deleted payment methods.
   * <p><strong>Currently Supported Types:</strong></p>
   * <ul>
   * <li><code>fedwire</code>: Domestic USD wire transfers</li>
   * <li><code>swift</code>: International wire transfers</li>
   * <li><code>sepa</code>: SEPA EUR transfers</li>
   * </ul>
   * <p><strong>Note:</strong> Payment methods are created and verified through your linked CDP entity. Currently, fetching payment methods is only supported for Prime investment vehicles linked to CDP.</p>
   */
  public CompletableFuture<CdpClientHttpResponse<ListPaymentMethodsResponse>> listPaymentMethods() {
    return listPaymentMethods(ListPaymentMethodsRequest.builder().build());
  }

  /**
   * List payment methods linked to your entity. Payment methods represent external financial instruments that can be used as a target for transfers. The list will not include disabled or deleted payment methods.
   * <p><strong>Currently Supported Types:</strong></p>
   * <ul>
   * <li><code>fedwire</code>: Domestic USD wire transfers</li>
   * <li><code>swift</code>: International wire transfers</li>
   * <li><code>sepa</code>: SEPA EUR transfers</li>
   * </ul>
   * <p><strong>Note:</strong> Payment methods are created and verified through your linked CDP entity. Currently, fetching payment methods is only supported for Prime investment vehicles linked to CDP.</p>
   */
  public CompletableFuture<CdpClientHttpResponse<ListPaymentMethodsResponse>> listPaymentMethods(
      RequestOptions requestOptions) {
    return listPaymentMethods(ListPaymentMethodsRequest.builder().build(),requestOptions);
  }

  /**
   * List payment methods linked to your entity. Payment methods represent external financial instruments that can be used as a target for transfers. The list will not include disabled or deleted payment methods.
   * <p><strong>Currently Supported Types:</strong></p>
   * <ul>
   * <li><code>fedwire</code>: Domestic USD wire transfers</li>
   * <li><code>swift</code>: International wire transfers</li>
   * <li><code>sepa</code>: SEPA EUR transfers</li>
   * </ul>
   * <p><strong>Note:</strong> Payment methods are created and verified through your linked CDP entity. Currently, fetching payment methods is only supported for Prime investment vehicles linked to CDP.</p>
   */
  public CompletableFuture<CdpClientHttpResponse<ListPaymentMethodsResponse>> listPaymentMethods(
      ListPaymentMethodsRequest request) {
    return listPaymentMethods(request,null);
  }

  /**
   * List payment methods linked to your entity. Payment methods represent external financial instruments that can be used as a target for transfers. The list will not include disabled or deleted payment methods.
   * <p><strong>Currently Supported Types:</strong></p>
   * <ul>
   * <li><code>fedwire</code>: Domestic USD wire transfers</li>
   * <li><code>swift</code>: International wire transfers</li>
   * <li><code>sepa</code>: SEPA EUR transfers</li>
   * </ul>
   * <p><strong>Note:</strong> Payment methods are created and verified through your linked CDP entity. Currently, fetching payment methods is only supported for Prime investment vehicles linked to CDP.</p>
   */
  public CompletableFuture<CdpClientHttpResponse<ListPaymentMethodsResponse>> listPaymentMethods(
      ListPaymentMethodsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v2/payment-methods");if (request.getPageSize().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageSize", request.getPageSize().get(), false);
      }
      if (request.getPageToken().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageToken", request.getPageToken().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<CdpClientHttpResponse<ListPaymentMethodsResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListPaymentMethodsResponse.class), response));
              return;
            }
            try {
              switch (response.code()) {
                case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                return;
                case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                return;
                case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new CdpClientException("Failed to decode HTTP response", e));
          }
          catch (IOException e) {
            future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Get details of a specific payment method by its ID. Returns 404 if the payment method is not found or not owned by the requesting entity.
     */
    public CompletableFuture<CdpClientHttpResponse<PaymentMethodsPaymentMethod>> getPaymentMethod(
        PaymentMethodId paymentMethodId) {
      return getPaymentMethod(paymentMethodId,GetPaymentMethodRequest.builder().build());
    }

    /**
     * Get details of a specific payment method by its ID. Returns 404 if the payment method is not found or not owned by the requesting entity.
     */
    public CompletableFuture<CdpClientHttpResponse<PaymentMethodsPaymentMethod>> getPaymentMethod(
        PaymentMethodId paymentMethodId, RequestOptions requestOptions) {
      return getPaymentMethod(paymentMethodId,GetPaymentMethodRequest.builder().build(),requestOptions);
    }

    /**
     * Get details of a specific payment method by its ID. Returns 404 if the payment method is not found or not owned by the requesting entity.
     */
    public CompletableFuture<CdpClientHttpResponse<PaymentMethodsPaymentMethod>> getPaymentMethod(
        PaymentMethodId paymentMethodId, GetPaymentMethodRequest request) {
      return getPaymentMethod(paymentMethodId,request,null);
    }

    /**
     * Get details of a specific payment method by its ID. Returns 404 if the payment method is not found or not owned by the requesting entity.
     */
    public CompletableFuture<CdpClientHttpResponse<PaymentMethodsPaymentMethod>> getPaymentMethod(
        PaymentMethodId paymentMethodId, GetPaymentMethodRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v2/payment-methods")
        .addPathSegment(paymentMethodId.toString());if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("GET", null)
          .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CdpClientHttpResponse<PaymentMethodsPaymentMethod>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, PaymentMethodsPaymentMethod.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                  return;
                  case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                  return;
                  case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                  return;
                  case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new CdpClientException("Failed to decode HTTP response", e));
            }
            catch (IOException e) {
              future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
          }
        });
        return future;
      }
    }
