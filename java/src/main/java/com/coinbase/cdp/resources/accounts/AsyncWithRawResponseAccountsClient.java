/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.resources.accounts;

import com.coinbase.cdp.core.CdpClientApiException;
import com.coinbase.cdp.core.CdpClientException;
import com.coinbase.cdp.core.CdpClientHttpResponse;
import com.coinbase.cdp.core.ClientOptions;
import com.coinbase.cdp.core.EndpointMetadata;
import com.coinbase.cdp.core.MediaTypes;
import com.coinbase.cdp.core.ObjectMappers;
import com.coinbase.cdp.core.QueryStringMapper;
import com.coinbase.cdp.core.RequestOptions;
import com.coinbase.cdp.errors.BadRequestError;
import com.coinbase.cdp.errors.ForbiddenError;
import com.coinbase.cdp.errors.InternalServerError;
import com.coinbase.cdp.errors.NotFoundError;
import com.coinbase.cdp.errors.ServiceUnavailableError;
import com.coinbase.cdp.errors.UnauthorizedError;
import com.coinbase.cdp.errors.UnprocessableEntityError;
import com.coinbase.cdp.resources.accounts.requests.CreateAccountRequest;
import com.coinbase.cdp.resources.accounts.requests.GetBalanceByAssetRequest;
import com.coinbase.cdp.resources.accounts.requests.GetFoundationAccountByIdRequest;
import com.coinbase.cdp.resources.accounts.requests.ListBalancesRequest;
import com.coinbase.cdp.resources.accounts.requests.ListFoundationAccountsRequest;
import com.coinbase.cdp.resources.accounts.types.ListBalancesResponse;
import com.coinbase.cdp.resources.accounts.types.ListFoundationAccountsResponse;
import com.coinbase.cdp.types.Account;
import com.coinbase.cdp.types.AccountId;
import com.coinbase.cdp.types.Asset;
import com.coinbase.cdp.types.Balance;
import com.coinbase.cdp.types.Error;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.Override;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.Collections;
import java.util.concurrent.CompletableFuture;
import okhttp3.Call;
import okhttp3.Callback;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;
import org.jetbrains.annotations.NotNull;

public class AsyncWithRawResponseAccountsClient {
  protected final ClientOptions clientOptions;

  AsyncWithRawResponseAccountsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * List all accounts. The API will return all accounts that the API Key has Permissions to access. You can filter the results by using query parameters, which will be treated as a single conjunction (i.e. AND). Results are sorted by creation date in descending order (newest first).
   */
  public CompletableFuture<CdpClientHttpResponse<ListFoundationAccountsResponse>> listFoundationAccounts(
      ) {
    return listFoundationAccounts(ListFoundationAccountsRequest.builder().build());
  }

  /**
   * List all accounts. The API will return all accounts that the API Key has Permissions to access. You can filter the results by using query parameters, which will be treated as a single conjunction (i.e. AND). Results are sorted by creation date in descending order (newest first).
   */
  public CompletableFuture<CdpClientHttpResponse<ListFoundationAccountsResponse>> listFoundationAccounts(
      RequestOptions requestOptions) {
    return listFoundationAccounts(ListFoundationAccountsRequest.builder().build(),requestOptions);
  }

  /**
   * List all accounts. The API will return all accounts that the API Key has Permissions to access. You can filter the results by using query parameters, which will be treated as a single conjunction (i.e. AND). Results are sorted by creation date in descending order (newest first).
   */
  public CompletableFuture<CdpClientHttpResponse<ListFoundationAccountsResponse>> listFoundationAccounts(
      ListFoundationAccountsRequest request) {
    return listFoundationAccounts(request,null);
  }

  /**
   * List all accounts. The API will return all accounts that the API Key has Permissions to access. You can filter the results by using query parameters, which will be treated as a single conjunction (i.e. AND). Results are sorted by creation date in descending order (newest first).
   */
  public CompletableFuture<CdpClientHttpResponse<ListFoundationAccountsResponse>> listFoundationAccounts(
      ListFoundationAccountsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v2/accounts");if (request.getPageSize().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageSize", request.getPageSize().get(), false);
      }
      if (request.getPageToken().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageToken", request.getPageToken().get(), false);
      }
      if (request.getType().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "type", request.getType().get(), false);
      }
      if (request.getOwner().isPresent()) {
        QueryStringMapper.addQueryParameter(
            httpUrl, "owner", String.join(",", request.getOwner().get()), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      CompletableFuture<CdpClientHttpResponse<ListFoundationAccountsResponse>> future = new CompletableFuture<>();
      client.newCall(okhttpRequest).enqueue(new Callback() {
        @Override
        public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
          try (ResponseBody responseBody = response.body()) {
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              future.complete(new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListFoundationAccountsResponse.class), response));
              return;
            }
            try {
              if (response.code() == 400) {
                future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                return;
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            future.completeExceptionally(new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response));
            return;
          }
          catch (JsonProcessingException e) {
            future.completeExceptionally(new CdpClientException("Failed to decode HTTP response", e));
          }
          catch (IOException e) {
            future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
          }
        }

        @Override
        public void onFailure(@NotNull Call call, @NotNull IOException e) {
          future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
        }
      });
      return future;
    }

    /**
     * Create an account. Two ownership modes are supported:
     * <ul>
     * <li>
     * <p><strong>Entity-owned</strong>: when <code>owner</code> is omitted, the account is owned by the
     * Entity making the request. Returns an account with <code>owner: entity_&lt;uuid&gt;</code>.</p>
     * </li>
     * <li>
     * <p><strong>Customer-owned</strong>: pass a Customer ID as <code>owner</code>
     * (e.g. <code>customer_af2937b0-9846-4fe7-bfe9-ccc22d935114</code>). The Customer
     * must have the <code>custodyCrypto</code>, <code>custodyFiat</code>, and <code>custodyStablecoin</code>
     * capabilities enabled, otherwise the request is rejected with
     * <code>customer_not_authorized</code> (HTTP 403).</p>
     * </li>
     * </ul>
     */
    public CompletableFuture<CdpClientHttpResponse<Account>> createFoundationAccount() {
      return createFoundationAccount(CreateAccountRequest.builder().build());
    }

    /**
     * Create an account. Two ownership modes are supported:
     * <ul>
     * <li>
     * <p><strong>Entity-owned</strong>: when <code>owner</code> is omitted, the account is owned by the
     * Entity making the request. Returns an account with <code>owner: entity_&lt;uuid&gt;</code>.</p>
     * </li>
     * <li>
     * <p><strong>Customer-owned</strong>: pass a Customer ID as <code>owner</code>
     * (e.g. <code>customer_af2937b0-9846-4fe7-bfe9-ccc22d935114</code>). The Customer
     * must have the <code>custodyCrypto</code>, <code>custodyFiat</code>, and <code>custodyStablecoin</code>
     * capabilities enabled, otherwise the request is rejected with
     * <code>customer_not_authorized</code> (HTTP 403).</p>
     * </li>
     * </ul>
     */
    public CompletableFuture<CdpClientHttpResponse<Account>> createFoundationAccount(
        RequestOptions requestOptions) {
      return createFoundationAccount(CreateAccountRequest.builder().build(),requestOptions);
    }

    /**
     * Create an account. Two ownership modes are supported:
     * <ul>
     * <li>
     * <p><strong>Entity-owned</strong>: when <code>owner</code> is omitted, the account is owned by the
     * Entity making the request. Returns an account with <code>owner: entity_&lt;uuid&gt;</code>.</p>
     * </li>
     * <li>
     * <p><strong>Customer-owned</strong>: pass a Customer ID as <code>owner</code>
     * (e.g. <code>customer_af2937b0-9846-4fe7-bfe9-ccc22d935114</code>). The Customer
     * must have the <code>custodyCrypto</code>, <code>custodyFiat</code>, and <code>custodyStablecoin</code>
     * capabilities enabled, otherwise the request is rejected with
     * <code>customer_not_authorized</code> (HTTP 403).</p>
     * </li>
     * </ul>
     */
    public CompletableFuture<CdpClientHttpResponse<Account>> createFoundationAccount(
        CreateAccountRequest request) {
      return createFoundationAccount(request,null);
    }

    /**
     * Create an account. Two ownership modes are supported:
     * <ul>
     * <li>
     * <p><strong>Entity-owned</strong>: when <code>owner</code> is omitted, the account is owned by the
     * Entity making the request. Returns an account with <code>owner: entity_&lt;uuid&gt;</code>.</p>
     * </li>
     * <li>
     * <p><strong>Customer-owned</strong>: pass a Customer ID as <code>owner</code>
     * (e.g. <code>customer_af2937b0-9846-4fe7-bfe9-ccc22d935114</code>). The Customer
     * must have the <code>custodyCrypto</code>, <code>custodyFiat</code>, and <code>custodyStablecoin</code>
     * capabilities enabled, otherwise the request is rejected with
     * <code>customer_not_authorized</code> (HTTP 403).</p>
     * </li>
     * </ul>
     */
    public CompletableFuture<CdpClientHttpResponse<Account>> createFoundationAccount(
        CreateAccountRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v2/accounts");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
          _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        CompletableFuture<CdpClientHttpResponse<Account>> future = new CompletableFuture<>();
        client.newCall(okhttpRequest).enqueue(new Callback() {
          @Override
          public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
            try (ResponseBody responseBody = response.body()) {
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                future.complete(new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Account.class), response));
                return;
              }
              try {
                switch (response.code()) {
                  case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                  return;
                  case 403:future.completeExceptionally(new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                  return;
                  case 422:future.completeExceptionally(new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                  return;
                  case 503:future.completeExceptionally(new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                  return;
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              future.completeExceptionally(new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response));
              return;
            }
            catch (JsonProcessingException e) {
              future.completeExceptionally(new CdpClientException("Failed to decode HTTP response", e));
            }
            catch (IOException e) {
              future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
            }
          }

          @Override
          public void onFailure(@NotNull Call call, @NotNull IOException e) {
            future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
          }
        });
        return future;
      }

      /**
       * Get an account by its ID.
       */
      public CompletableFuture<CdpClientHttpResponse<Account>> getFoundationAccountById(
          AccountId accountId) {
        return getFoundationAccountById(accountId,GetFoundationAccountByIdRequest.builder().build());
      }

      /**
       * Get an account by its ID.
       */
      public CompletableFuture<CdpClientHttpResponse<Account>> getFoundationAccountById(
          AccountId accountId, RequestOptions requestOptions) {
        return getFoundationAccountById(accountId,GetFoundationAccountByIdRequest.builder().build(),requestOptions);
      }

      /**
       * Get an account by its ID.
       */
      public CompletableFuture<CdpClientHttpResponse<Account>> getFoundationAccountById(
          AccountId accountId, GetFoundationAccountByIdRequest request) {
        return getFoundationAccountById(accountId,request,null);
      }

      /**
       * Get an account by its ID.
       */
      public CompletableFuture<CdpClientHttpResponse<Account>> getFoundationAccountById(
          AccountId accountId, GetFoundationAccountByIdRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v2/accounts")
          .addPathSegment(accountId.toString());if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          CompletableFuture<CdpClientHttpResponse<Account>> future = new CompletableFuture<>();
          client.newCall(okhttpRequest).enqueue(new Callback() {
            @Override
            public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
              try (ResponseBody responseBody = response.body()) {
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  future.complete(new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Account.class), response));
                  return;
                }
                try {
                  switch (response.code()) {
                    case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                    return;
                    case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                    return;
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                future.completeExceptionally(new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                return;
              }
              catch (JsonProcessingException e) {
                future.completeExceptionally(new CdpClientException("Failed to decode HTTP response", e));
              }
              catch (IOException e) {
                future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
              }
            }

            @Override
            public void onFailure(@NotNull Call call, @NotNull IOException e) {
              future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
            }
          });
          return future;
        }

        /**
         * List the balances for an account. Results are sorted by native-fiat equivalent balance in descending order.
         */
        public CompletableFuture<CdpClientHttpResponse<ListBalancesResponse>> listBalances(
            AccountId accountId) {
          return listBalances(accountId,ListBalancesRequest.builder().build());
        }

        /**
         * List the balances for an account. Results are sorted by native-fiat equivalent balance in descending order.
         */
        public CompletableFuture<CdpClientHttpResponse<ListBalancesResponse>> listBalances(
            AccountId accountId, RequestOptions requestOptions) {
          return listBalances(accountId,ListBalancesRequest.builder().build(),requestOptions);
        }

        /**
         * List the balances for an account. Results are sorted by native-fiat equivalent balance in descending order.
         */
        public CompletableFuture<CdpClientHttpResponse<ListBalancesResponse>> listBalances(
            AccountId accountId, ListBalancesRequest request) {
          return listBalances(accountId,request,null);
        }

        /**
         * List the balances for an account. Results are sorted by native-fiat equivalent balance in descending order.
         */
        public CompletableFuture<CdpClientHttpResponse<ListBalancesResponse>> listBalances(
            AccountId accountId, ListBalancesRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v2/accounts")
            .addPathSegment(accountId.toString())
            .addPathSegments("balances");if (request.getPageSize().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "pageSize", request.getPageSize().get(), false);
            }
            if (request.getPageToken().isPresent()) {
              QueryStringMapper.addQueryParameter(httpUrl, "pageToken", request.getPageToken().get(), false);
            }
            if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("GET", null)
              .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            CompletableFuture<CdpClientHttpResponse<ListBalancesResponse>> future = new CompletableFuture<>();
            client.newCall(okhttpRequest).enqueue(new Callback() {
              @Override
              public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                try (ResponseBody responseBody = response.body()) {
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    future.complete(new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListBalancesResponse.class), response));
                    return;
                  }
                  try {
                    switch (response.code()) {
                      case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                      return;
                      case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                      return;
                      case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                      return;
                      case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                      return;
                      case 503:future.completeExceptionally(new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                      return;
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  future.completeExceptionally(new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                  return;
                }
                catch (JsonProcessingException e) {
                  future.completeExceptionally(new CdpClientException("Failed to decode HTTP response", e));
                }
                catch (IOException e) {
                  future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
                }
              }

              @Override
              public void onFailure(@NotNull Call call, @NotNull IOException e) {
                future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
              }
            });
            return future;
          }

          /**
           * Get the balance for an account by asset.
           */
          public CompletableFuture<CdpClientHttpResponse<Balance>> getBalanceByAsset(
              AccountId accountId, Asset asset) {
            return getBalanceByAsset(accountId,asset,GetBalanceByAssetRequest.builder().build());
          }

          /**
           * Get the balance for an account by asset.
           */
          public CompletableFuture<CdpClientHttpResponse<Balance>> getBalanceByAsset(
              AccountId accountId, Asset asset, RequestOptions requestOptions) {
            return getBalanceByAsset(accountId,asset,GetBalanceByAssetRequest.builder().build(),requestOptions);
          }

          /**
           * Get the balance for an account by asset.
           */
          public CompletableFuture<CdpClientHttpResponse<Balance>> getBalanceByAsset(
              AccountId accountId, Asset asset, GetBalanceByAssetRequest request) {
            return getBalanceByAsset(accountId,asset,request,null);
          }

          /**
           * Get the balance for an account by asset.
           */
          public CompletableFuture<CdpClientHttpResponse<Balance>> getBalanceByAsset(
              AccountId accountId, Asset asset, GetBalanceByAssetRequest request,
              RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("v2/accounts")
              .addPathSegment(accountId.toString())
              .addPathSegments("balances")
              .addPathSegment(asset.toString());if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              CompletableFuture<CdpClientHttpResponse<Balance>> future = new CompletableFuture<>();
              client.newCall(okhttpRequest).enqueue(new Callback() {
                @Override
                public void onResponse(@NotNull Call call, @NotNull Response response) throws IOException {
                  try (ResponseBody responseBody = response.body()) {
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      future.complete(new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Balance.class), response));
                      return;
                    }
                    try {
                      switch (response.code()) {
                        case 400:future.completeExceptionally(new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                        return;
                        case 401:future.completeExceptionally(new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                        return;
                        case 404:future.completeExceptionally(new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                        return;
                        case 500:future.completeExceptionally(new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                        return;
                        case 503:future.completeExceptionally(new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response));
                        return;
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    future.completeExceptionally(new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response));
                    return;
                  }
                  catch (JsonProcessingException e) {
                    future.completeExceptionally(new CdpClientException("Failed to decode HTTP response", e));
                  }
                  catch (IOException e) {
                    future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
                  }
                }

                @Override
                public void onFailure(@NotNull Call call, @NotNull IOException e) {
                  future.completeExceptionally(new CdpClientException("Network error executing HTTP request", e));
                }
              });
              return future;
            }
          }
