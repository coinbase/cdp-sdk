/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.resources.enduseraccountmanagement;

import com.coinbase.cdp.core.CdpClientApiException;
import com.coinbase.cdp.core.CdpClientException;
import com.coinbase.cdp.core.CdpClientHttpResponse;
import com.coinbase.cdp.core.ClientOptions;
import com.coinbase.cdp.core.EndpointMetadata;
import com.coinbase.cdp.core.MediaTypes;
import com.coinbase.cdp.core.ObjectMappers;
import com.coinbase.cdp.core.QueryStringMapper;
import com.coinbase.cdp.core.RequestOptions;
import com.coinbase.cdp.errors.BadGatewayError;
import com.coinbase.cdp.errors.BadRequestError;
import com.coinbase.cdp.errors.ConflictError;
import com.coinbase.cdp.errors.InternalServerError;
import com.coinbase.cdp.errors.NotFoundError;
import com.coinbase.cdp.errors.PaymentRequiredError;
import com.coinbase.cdp.errors.ServiceUnavailableError;
import com.coinbase.cdp.errors.UnauthorizedError;
import com.coinbase.cdp.errors.UnprocessableEntityError;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.AddEndUserEvmAccountRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.AddEndUserEvmSmartAccountRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.AddEndUserSolanaAccountRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.CreateEndUserRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.GetEndUserRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.ImportEndUserRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.ListEndUsersRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.LookupEndUserRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.ValidateEndUserAccessTokenRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.AddEndUserEvmAccountResponse;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.AddEndUserEvmSmartAccountResponse;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.AddEndUserSolanaAccountResponse;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.ListEndUsersResponse;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.LookupEndUserResponse;
import com.coinbase.cdp.types.EndUser;
import com.coinbase.cdp.types.Error;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.Arrays;
import java.util.Collections;
import java.util.Map;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class WithRawResponseEndUserAccountManagementClient {
  protected final ClientOptions clientOptions;

  WithRawResponseEndUserAccountManagementClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Lists the end users belonging to the developer's CDP Project.
   * By default, the response is sorted by creation date in ascending order and paginated to 20 users per page.
   */
  public CdpClientHttpResponse<ListEndUsersResponse> listEndUsers() {
    return listEndUsers(ListEndUsersRequest.builder().build());
  }

  /**
   * Lists the end users belonging to the developer's CDP Project.
   * By default, the response is sorted by creation date in ascending order and paginated to 20 users per page.
   */
  public CdpClientHttpResponse<ListEndUsersResponse> listEndUsers(RequestOptions requestOptions) {
    return listEndUsers(ListEndUsersRequest.builder().build(),requestOptions);
  }

  /**
   * Lists the end users belonging to the developer's CDP Project.
   * By default, the response is sorted by creation date in ascending order and paginated to 20 users per page.
   */
  public CdpClientHttpResponse<ListEndUsersResponse> listEndUsers(ListEndUsersRequest request) {
    return listEndUsers(request,null);
  }

  /**
   * Lists the end users belonging to the developer's CDP Project.
   * By default, the response is sorted by creation date in ascending order and paginated to 20 users per page.
   */
  public CdpClientHttpResponse<ListEndUsersResponse> listEndUsers(ListEndUsersRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v2/end-users");if (request.getPageSize().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageSize", request.getPageSize().get(), false);
      }
      if (request.getPageToken().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageToken", request.getPageToken().get(), false);
      }
      if (request.getSort().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sort", request.getSort().get(), true);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListEndUsersResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new CdpClientException("Failed to decode HTTP response", e);
      }
      catch (IOException e) {
        throw new CdpClientException("Network error executing HTTP request", e);
      }
    }

    /**
     * Creates an end user. An end user is an entity that can own CDP EVM accounts, EVM smart accounts, and/or Solana accounts. 1 or more authentication methods must be associated with an end user. By default, no accounts are created unless the optional <code>evmAccount</code> and/or <code>solanaAccount</code> fields are provided.
     * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
     */
    public CdpClientHttpResponse<EndUser> createEndUser(CreateEndUserRequest request) {
      return createEndUser(request,null);
    }

    /**
     * Creates an end user. An end user is an entity that can own CDP EVM accounts, EVM smart accounts, and/or Solana accounts. 1 or more authentication methods must be associated with an end user. By default, no accounts are created unless the optional <code>evmAccount</code> and/or <code>solanaAccount</code> fields are provided.
     * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
     */
    public CdpClientHttpResponse<EndUser> createEndUser(CreateEndUserRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v2/end-users");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Arrays.asList("X-Wallet-Auth")))
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
          _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EndUser.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 402:throw new PaymentRequiredError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new CdpClientException("Failed to decode HTTP response", e);
        }
        catch (IOException e) {
          throw new CdpClientException("Network error executing HTTP request", e);
        }
      }

      /**
       * Validates the end user's access token and returns the end user's information. Returns an error if the access token is invalid or expired.
       * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
       */
      public CdpClientHttpResponse<EndUser> validateEndUserAccessToken(
          ValidateEndUserAccessTokenRequest request) {
        return validateEndUserAccessToken(request,null);
      }

      /**
       * Validates the end user's access token and returns the end user's information. Returns an error if the access token is invalid or expired.
       * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
       */
      public CdpClientHttpResponse<EndUser> validateEndUserAccessToken(
          ValidateEndUserAccessTokenRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v2/end-users/auth/validate-token");if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          RequestBody body;
          try {
            body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
          }
          catch(JsonProcessingException e) {
            throw new CdpClientException("Failed to serialize request", e);
          }
          Request okhttpRequest = new Request.Builder()
            .url(httpUrl.build())
            .method("POST", body)
            .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Content-Type", "application/json")
            .addHeader("Accept", "application/json")
            .build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EndUser.class), response);
            }
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (JsonProcessingException e) {
            throw new CdpClientException("Failed to decode HTTP response", e);
          }
          catch (IOException e) {
            throw new CdpClientException("Network error executing HTTP request", e);
          }
        }

        /**
         * Gets an end user by ID.
         * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
         */
        public CdpClientHttpResponse<EndUser> getEndUser(String userId) {
          return getEndUser(userId,GetEndUserRequest.builder().build());
        }

        /**
         * Gets an end user by ID.
         * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
         */
        public CdpClientHttpResponse<EndUser> getEndUser(String userId,
            RequestOptions requestOptions) {
          return getEndUser(userId,GetEndUserRequest.builder().build(),requestOptions);
        }

        /**
         * Gets an end user by ID.
         * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
         */
        public CdpClientHttpResponse<EndUser> getEndUser(String userId, GetEndUserRequest request) {
          return getEndUser(userId,request,null);
        }

        /**
         * Gets an end user by ID.
         * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
         */
        public CdpClientHttpResponse<EndUser> getEndUser(String userId, GetEndUserRequest request,
            RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v2/end-users")
            .addPathSegment(userId);if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("GET", null)
              .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EndUser.class), response);
              }
              try {
                switch (response.code()) {
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (JsonProcessingException e) {
              throw new CdpClientException("Failed to decode HTTP response", e);
            }
            catch (IOException e) {
              throw new CdpClientException("Network error executing HTTP request", e);
            }
          }

          /**
           * Looks up end users. Exactly one lookup type must be provided per request:
           * <ul>
           * <li>
           * <p><strong>email</strong>: searches across all email-based authentication methods
           * (email, Google, Apple, GitHub). May return multiple end users if the
           * same email address appears across different auth methods.</p>
           * </li>
           * <li>
           * <p><strong>oauthProvider + oauthSubject</strong>: looks up a user by their OAuth
           * provider and subject (the <code>sub</code> claim from the provider's ID token).
           * Both params must be provided together.</p>
           * </li>
           * <li>
           * <p><strong>phoneNumber</strong>: looks up a user by their SMS-verified phone number.</p>
           * </li>
           * <li>
           * <p><strong>siweAddress</strong>: looks up a user by the Ethereum address they authenticated
           * with via Sign In With Ethereum (EIP-4361).</p>
           * </li>
           * </ul>
           * <p>Returns all matching end users. If no end users match, an empty array is returned.</p>
           * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
           */
          public CdpClientHttpResponse<LookupEndUserResponse> lookupEndUser() {
            return lookupEndUser(LookupEndUserRequest.builder().build());
          }

          /**
           * Looks up end users. Exactly one lookup type must be provided per request:
           * <ul>
           * <li>
           * <p><strong>email</strong>: searches across all email-based authentication methods
           * (email, Google, Apple, GitHub). May return multiple end users if the
           * same email address appears across different auth methods.</p>
           * </li>
           * <li>
           * <p><strong>oauthProvider + oauthSubject</strong>: looks up a user by their OAuth
           * provider and subject (the <code>sub</code> claim from the provider's ID token).
           * Both params must be provided together.</p>
           * </li>
           * <li>
           * <p><strong>phoneNumber</strong>: looks up a user by their SMS-verified phone number.</p>
           * </li>
           * <li>
           * <p><strong>siweAddress</strong>: looks up a user by the Ethereum address they authenticated
           * with via Sign In With Ethereum (EIP-4361).</p>
           * </li>
           * </ul>
           * <p>Returns all matching end users. If no end users match, an empty array is returned.</p>
           * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
           */
          public CdpClientHttpResponse<LookupEndUserResponse> lookupEndUser(
              RequestOptions requestOptions) {
            return lookupEndUser(LookupEndUserRequest.builder().build(),requestOptions);
          }

          /**
           * Looks up end users. Exactly one lookup type must be provided per request:
           * <ul>
           * <li>
           * <p><strong>email</strong>: searches across all email-based authentication methods
           * (email, Google, Apple, GitHub). May return multiple end users if the
           * same email address appears across different auth methods.</p>
           * </li>
           * <li>
           * <p><strong>oauthProvider + oauthSubject</strong>: looks up a user by their OAuth
           * provider and subject (the <code>sub</code> claim from the provider's ID token).
           * Both params must be provided together.</p>
           * </li>
           * <li>
           * <p><strong>phoneNumber</strong>: looks up a user by their SMS-verified phone number.</p>
           * </li>
           * <li>
           * <p><strong>siweAddress</strong>: looks up a user by the Ethereum address they authenticated
           * with via Sign In With Ethereum (EIP-4361).</p>
           * </li>
           * </ul>
           * <p>Returns all matching end users. If no end users match, an empty array is returned.</p>
           * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
           */
          public CdpClientHttpResponse<LookupEndUserResponse> lookupEndUser(
              LookupEndUserRequest request) {
            return lookupEndUser(request,null);
          }

          /**
           * Looks up end users. Exactly one lookup type must be provided per request:
           * <ul>
           * <li>
           * <p><strong>email</strong>: searches across all email-based authentication methods
           * (email, Google, Apple, GitHub). May return multiple end users if the
           * same email address appears across different auth methods.</p>
           * </li>
           * <li>
           * <p><strong>oauthProvider + oauthSubject</strong>: looks up a user by their OAuth
           * provider and subject (the <code>sub</code> claim from the provider's ID token).
           * Both params must be provided together.</p>
           * </li>
           * <li>
           * <p><strong>phoneNumber</strong>: looks up a user by their SMS-verified phone number.</p>
           * </li>
           * <li>
           * <p><strong>siweAddress</strong>: looks up a user by the Ethereum address they authenticated
           * with via Sign In With Ethereum (EIP-4361).</p>
           * </li>
           * </ul>
           * <p>Returns all matching end users. If no end users match, an empty array is returned.</p>
           * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
           */
          public CdpClientHttpResponse<LookupEndUserResponse> lookupEndUser(
              LookupEndUserRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("v2/end-users/lookup");if (request.getEmail().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "email", request.getEmail().get(), false);
              }
              if (request.getOauthProvider().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "oauthProvider", request.getOauthProvider().get(), false);
              }
              if (request.getOauthSubject().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "oauthSubject", request.getOauthSubject().get(), false);
              }
              if (request.getPhoneNumber().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "phoneNumber", request.getPhoneNumber().get(), false);
              }
              if (request.getSiweAddress().isPresent()) {
                QueryStringMapper.addQueryParameter(httpUrl, "siweAddress", request.getSiweAddress().get(), false);
              }
              if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, LookupEndUserResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (JsonProcessingException e) {
                throw new CdpClientException("Failed to decode HTTP response", e);
              }
              catch (IOException e) {
                throw new CdpClientException("Network error executing HTTP request", e);
              }
            }

            /**
             * Adds a new EVM EOA account to an existing end user. End users can have up to 10 EVM accounts.
             * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
             */
            public CdpClientHttpResponse<AddEndUserEvmAccountResponse> addEndUserEvmAccount(
                String userId, Map<String, Object> body) {
              return addEndUserEvmAccount(userId, AddEndUserEvmAccountRequest.builder().body(body).build());
            }

            /**
             * Adds a new EVM EOA account to an existing end user. End users can have up to 10 EVM accounts.
             * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
             */
            public CdpClientHttpResponse<AddEndUserEvmAccountResponse> addEndUserEvmAccount(
                String userId, Map<String, Object> body, RequestOptions requestOptions) {
              return addEndUserEvmAccount(userId, AddEndUserEvmAccountRequest.builder().body(body).build(), requestOptions);
            }

            /**
             * Adds a new EVM EOA account to an existing end user. End users can have up to 10 EVM accounts.
             * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
             */
            public CdpClientHttpResponse<AddEndUserEvmAccountResponse> addEndUserEvmAccount(
                String userId, AddEndUserEvmAccountRequest request) {
              return addEndUserEvmAccount(userId,request,null);
            }

            /**
             * Adds a new EVM EOA account to an existing end user. End users can have up to 10 EVM accounts.
             * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
             */
            public CdpClientHttpResponse<AddEndUserEvmAccountResponse> addEndUserEvmAccount(
                String userId, AddEndUserEvmAccountRequest request, RequestOptions requestOptions) {
              HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                .addPathSegments("v2/end-users")
                .addPathSegment(userId)
                .addPathSegments("evm");if (requestOptions != null) {
                  requestOptions.getQueryParameters().forEach((_key, _value) -> {
                    httpUrl.addQueryParameter(_key, _value);
                  } );
                }
                RequestBody body;
                try {
                  body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
                }
                catch(Exception e) {
                  throw new RuntimeException(e);
                }
                Request.Builder _requestBuilder = new Request.Builder()
                  .url(httpUrl.build())
                  .method("POST", body)
                  .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Arrays.asList("X-Wallet-Auth")))
                  .headers(Headers.of(clientOptions.headers(requestOptions)))
                  .addHeader("Content-Type", "application/json")
                  .addHeader("Accept", "application/json");
                if (request.getIdempotencyKey().isPresent()) {
                  _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
                }
                Request okhttpRequest = _requestBuilder.build();
                OkHttpClient client = clientOptions.httpClient();
                if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                  client = clientOptions.httpClientWithTimeout(requestOptions);
                }
                try (Response response = client.newCall(okhttpRequest).execute()) {
                  ResponseBody responseBody = response.body();
                  String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                  if (response.isSuccessful()) {
                    return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AddEndUserEvmAccountResponse.class), response);
                  }
                  try {
                    switch (response.code()) {
                      case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                      case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                      case 402:throw new PaymentRequiredError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                      case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                      case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                      case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                      case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                      case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    }
                  }
                  catch (JsonProcessingException ignored) {
                    // unable to map error response, throwing generic error
                  }
                  Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                  throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                }
                catch (JsonProcessingException e) {
                  throw new CdpClientException("Failed to decode HTTP response", e);
                }
                catch (IOException e) {
                  throw new CdpClientException("Network error executing HTTP request", e);
                }
              }

              /**
               * Creates an EVM smart account for an existing end user. The backend will create a new EVM EOA account to serve as the owner of the smart account.
               * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
               */
              public CdpClientHttpResponse<AddEndUserEvmSmartAccountResponse> addEndUserEvmSmartAccount(
                  String userId) {
                return addEndUserEvmSmartAccount(userId,AddEndUserEvmSmartAccountRequest.builder().build());
              }

              /**
               * Creates an EVM smart account for an existing end user. The backend will create a new EVM EOA account to serve as the owner of the smart account.
               * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
               */
              public CdpClientHttpResponse<AddEndUserEvmSmartAccountResponse> addEndUserEvmSmartAccount(
                  String userId, RequestOptions requestOptions) {
                return addEndUserEvmSmartAccount(userId,AddEndUserEvmSmartAccountRequest.builder().build(),requestOptions);
              }

              /**
               * Creates an EVM smart account for an existing end user. The backend will create a new EVM EOA account to serve as the owner of the smart account.
               * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
               */
              public CdpClientHttpResponse<AddEndUserEvmSmartAccountResponse> addEndUserEvmSmartAccount(
                  String userId, AddEndUserEvmSmartAccountRequest request) {
                return addEndUserEvmSmartAccount(userId,request,null);
              }

              /**
               * Creates an EVM smart account for an existing end user. The backend will create a new EVM EOA account to serve as the owner of the smart account.
               * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
               */
              public CdpClientHttpResponse<AddEndUserEvmSmartAccountResponse> addEndUserEvmSmartAccount(
                  String userId, AddEndUserEvmSmartAccountRequest request,
                  RequestOptions requestOptions) {
                HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                  .addPathSegments("v2/end-users")
                  .addPathSegment(userId)
                  .addPathSegments("evm-smart-account");if (requestOptions != null) {
                    requestOptions.getQueryParameters().forEach((_key, _value) -> {
                      httpUrl.addQueryParameter(_key, _value);
                    } );
                  }
                  RequestBody body;
                  try {
                    body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                  }
                  catch(Exception e) {
                    throw new RuntimeException(e);
                  }
                  Request.Builder _requestBuilder = new Request.Builder()
                    .url(httpUrl.build())
                    .method("POST", body)
                    .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Arrays.asList("X-Wallet-Auth")))
                    .headers(Headers.of(clientOptions.headers(requestOptions)))
                    .addHeader("Content-Type", "application/json")
                    .addHeader("Accept", "application/json");
                  if (request.getIdempotencyKey().isPresent()) {
                    _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
                  }
                  Request okhttpRequest = _requestBuilder.build();
                  OkHttpClient client = clientOptions.httpClient();
                  if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                    client = clientOptions.httpClientWithTimeout(requestOptions);
                  }
                  try (Response response = client.newCall(okhttpRequest).execute()) {
                    ResponseBody responseBody = response.body();
                    String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                    if (response.isSuccessful()) {
                      return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AddEndUserEvmSmartAccountResponse.class), response);
                    }
                    try {
                      switch (response.code()) {
                        case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                        case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                        case 402:throw new PaymentRequiredError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                        case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                        case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                        case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                        case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                        case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                      }
                    }
                    catch (JsonProcessingException ignored) {
                      // unable to map error response, throwing generic error
                    }
                    Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                    throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                  }
                  catch (JsonProcessingException e) {
                    throw new CdpClientException("Failed to decode HTTP response", e);
                  }
                  catch (IOException e) {
                    throw new CdpClientException("Network error executing HTTP request", e);
                  }
                }

                /**
                 * Adds a new Solana account to an existing end user. End users can have up to 10 Solana accounts.
                 * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
                 */
                public CdpClientHttpResponse<AddEndUserSolanaAccountResponse> addEndUserSolanaAccount(
                    String userId, Map<String, Object> body) {
                  return addEndUserSolanaAccount(userId, AddEndUserSolanaAccountRequest.builder().body(body).build());
                }

                /**
                 * Adds a new Solana account to an existing end user. End users can have up to 10 Solana accounts.
                 * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
                 */
                public CdpClientHttpResponse<AddEndUserSolanaAccountResponse> addEndUserSolanaAccount(
                    String userId, Map<String, Object> body, RequestOptions requestOptions) {
                  return addEndUserSolanaAccount(userId, AddEndUserSolanaAccountRequest.builder().body(body).build(), requestOptions);
                }

                /**
                 * Adds a new Solana account to an existing end user. End users can have up to 10 Solana accounts.
                 * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
                 */
                public CdpClientHttpResponse<AddEndUserSolanaAccountResponse> addEndUserSolanaAccount(
                    String userId, AddEndUserSolanaAccountRequest request) {
                  return addEndUserSolanaAccount(userId,request,null);
                }

                /**
                 * Adds a new Solana account to an existing end user. End users can have up to 10 Solana accounts.
                 * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
                 */
                public CdpClientHttpResponse<AddEndUserSolanaAccountResponse> addEndUserSolanaAccount(
                    String userId, AddEndUserSolanaAccountRequest request,
                    RequestOptions requestOptions) {
                  HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                    .addPathSegments("v2/end-users")
                    .addPathSegment(userId)
                    .addPathSegments("solana");if (requestOptions != null) {
                      requestOptions.getQueryParameters().forEach((_key, _value) -> {
                        httpUrl.addQueryParameter(_key, _value);
                      } );
                    }
                    RequestBody body;
                    try {
                      body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
                    }
                    catch(Exception e) {
                      throw new RuntimeException(e);
                    }
                    Request.Builder _requestBuilder = new Request.Builder()
                      .url(httpUrl.build())
                      .method("POST", body)
                      .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Arrays.asList("X-Wallet-Auth")))
                      .headers(Headers.of(clientOptions.headers(requestOptions)))
                      .addHeader("Content-Type", "application/json")
                      .addHeader("Accept", "application/json");
                    if (request.getIdempotencyKey().isPresent()) {
                      _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
                    }
                    Request okhttpRequest = _requestBuilder.build();
                    OkHttpClient client = clientOptions.httpClient();
                    if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                      client = clientOptions.httpClientWithTimeout(requestOptions);
                    }
                    try (Response response = client.newCall(okhttpRequest).execute()) {
                      ResponseBody responseBody = response.body();
                      String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                      if (response.isSuccessful()) {
                        return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, AddEndUserSolanaAccountResponse.class), response);
                      }
                      try {
                        switch (response.code()) {
                          case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                          case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                          case 402:throw new PaymentRequiredError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                          case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                          case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                          case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                          case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                          case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                        }
                      }
                      catch (JsonProcessingException ignored) {
                        // unable to map error response, throwing generic error
                      }
                      Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                      throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                    }
                    catch (JsonProcessingException e) {
                      throw new CdpClientException("Failed to decode HTTP response", e);
                    }
                    catch (IOException e) {
                      throw new CdpClientException("Network error executing HTTP request", e);
                    }
                  }

                  /**
                   * Imports an existing private key for an end user into the developer's CDP Project. The private key must be encrypted using the CDP SDK's encryption scheme before being sent to this endpoint. This API should be called from the <a href="https://github.com/coinbase/cdp-sdk">CDP SDK</a> to ensure that the associated private key is properly encrypted.
                   * <p>This endpoint allows developers to import existing keys for their end users, supporting both EVM and Solana key types. The end user must have at least one authentication method configured.</p>
                   */
                  public CdpClientHttpResponse<EndUser> importEndUser(
                      ImportEndUserRequest request) {
                    return importEndUser(request,null);
                  }

                  /**
                   * Imports an existing private key for an end user into the developer's CDP Project. The private key must be encrypted using the CDP SDK's encryption scheme before being sent to this endpoint. This API should be called from the <a href="https://github.com/coinbase/cdp-sdk">CDP SDK</a> to ensure that the associated private key is properly encrypted.
                   * <p>This endpoint allows developers to import existing keys for their end users, supporting both EVM and Solana key types. The end user must have at least one authentication method configured.</p>
                   */
                  public CdpClientHttpResponse<EndUser> importEndUser(ImportEndUserRequest request,
                      RequestOptions requestOptions) {
                    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

                      .addPathSegments("v2/end-users/import");if (requestOptions != null) {
                        requestOptions.getQueryParameters().forEach((_key, _value) -> {
                          httpUrl.addQueryParameter(_key, _value);
                        } );
                      }
                      RequestBody body;
                      try {
                        body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
                      }
                      catch(Exception e) {
                        throw new RuntimeException(e);
                      }
                      Request.Builder _requestBuilder = new Request.Builder()
                        .url(httpUrl.build())
                        .method("POST", body)
                        .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Arrays.asList("X-Wallet-Auth")))
                        .headers(Headers.of(clientOptions.headers(requestOptions)))
                        .addHeader("Content-Type", "application/json")
                        .addHeader("Accept", "application/json");
                      if (request.getIdempotencyKey().isPresent()) {
                        _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
                      }
                      Request okhttpRequest = _requestBuilder.build();
                      OkHttpClient client = clientOptions.httpClient();
                      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                        client = clientOptions.httpClientWithTimeout(requestOptions);
                      }
                      try (Response response = client.newCall(okhttpRequest).execute()) {
                        ResponseBody responseBody = response.body();
                        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                        if (response.isSuccessful()) {
                          return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, EndUser.class), response);
                        }
                        try {
                          switch (response.code()) {
                            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                            case 402:throw new PaymentRequiredError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                            case 409:throw new ConflictError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                            case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                            case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                            case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                          }
                        }
                        catch (JsonProcessingException ignored) {
                          // unable to map error response, throwing generic error
                        }
                        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                        throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
                      }
                      catch (JsonProcessingException e) {
                        throw new CdpClientException("Failed to decode HTTP response", e);
                      }
                      catch (IOException e) {
                        throw new CdpClientException("Network error executing HTTP request", e);
                      }
                    }
                  }
