/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.resources.transfers;

import com.coinbase.cdp.core.CdpClientApiException;
import com.coinbase.cdp.core.CdpClientException;
import com.coinbase.cdp.core.CdpClientHttpResponse;
import com.coinbase.cdp.core.ClientOptions;
import com.coinbase.cdp.core.EndpointMetadata;
import com.coinbase.cdp.core.MediaTypes;
import com.coinbase.cdp.core.ObjectMappers;
import com.coinbase.cdp.core.QueryStringMapper;
import com.coinbase.cdp.core.RequestOptions;
import com.coinbase.cdp.errors.BadGatewayError;
import com.coinbase.cdp.errors.BadRequestError;
import com.coinbase.cdp.errors.ForbiddenError;
import com.coinbase.cdp.errors.InternalServerError;
import com.coinbase.cdp.errors.NotFoundError;
import com.coinbase.cdp.errors.ServiceUnavailableError;
import com.coinbase.cdp.errors.TooManyRequestsError;
import com.coinbase.cdp.errors.UnauthorizedError;
import com.coinbase.cdp.errors.UnprocessableEntityError;
import com.coinbase.cdp.resources.transfers.requests.DepositTravelRuleRequest;
import com.coinbase.cdp.resources.transfers.requests.ExecuteFundTransferRequest;
import com.coinbase.cdp.resources.transfers.requests.GetTransferByIdRequest;
import com.coinbase.cdp.resources.transfers.requests.ListTransfersRequest;
import com.coinbase.cdp.resources.transfers.requests.TransferRequest;
import com.coinbase.cdp.resources.transfers.types.ListTransfersResponse;
import com.coinbase.cdp.types.DepositTravelRuleResponse;
import com.coinbase.cdp.types.Error;
import com.coinbase.cdp.types.Transfer;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.Collections;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class WithRawResponseTransfersClient {
  protected final ClientOptions clientOptions;

  WithRawResponseTransfersClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * List transfers for your organization. Use this to view and monitor your transfer activity.
   * <p><strong>Status Filtering</strong>: Filter by specific status to efficiently manage transfers:</p>
   * <ul>
   * <li><code>?status=processing</code> - Monitor active transfers.</li>
   * <li><code>?status=quoted</code> - Find transfers awaiting execution.</li>
   * <li><code>?status=failed</code> - Review failed transfers for troubleshooting.</li>
   * <li><code>?status=completed</code> - Find completed transfers.</li>
   * </ul>
   * <p><strong>Account Filtering</strong>: Filter by account ID to find transfers involving a specific account:</p>
   * <ul>
   * <li><code>?accountId=&lt;ID&gt;</code> - All transfers where the account is either source or target (OR semantics).</li>
   * <li><code>?sourceAccountId=&lt;ID&gt;</code> - Only transfers where the account is the source (outbound).</li>
   * <li><code>?targetAccountId=&lt;ID&gt;</code> - Only transfers where the account is the target (inbound).
   * Providing <code>accountId</code> together with <code>sourceAccountId</code> or <code>targetAccountId</code> is a validation error and returns HTTP 400.</li>
   * </ul>
   * <p><strong>Date Range Filtering</strong>: Filter by creation or last-updated time for reconciliation:</p>
   * <ul>
   * <li><code>?createdAfter=2026-01-01T00:00:00Z&amp;createdBefore=2026-01-31T23:59:59Z</code> - Transfers created within a date range.</li>
   * <li><code>?updatedAfter=2026-01-01T00:00:00Z</code> - Transfers updated since a given time. Useful for incremental sync.</li>
   * </ul>
   * <p><strong>Asset Filtering</strong>: Filter by source or target asset symbol:</p>
   * <ul>
   * <li><code>?sourceAsset=usd</code> - Transfers funded from a USD account.</li>
   * <li><code>?targetAsset=usdc</code> - Transfers delivering USDC to the target.
   * Supported asset symbols include <code>usdc</code>, <code>eurc</code>, <code>usd</code>, and <code>eur</code>.</li>
   * </ul>
   * <p><strong>Other Filters</strong>:</p>
   * <ul>
   * <li><code>?sourceAddress=0x...</code> - Transfers from a specific on-chain source address.</li>
   * <li><code>?targetAddress=0x...</code> - Transfers to a specific on-chain destination address.</li>
   * <li><code>?targetEmail=user@example.com</code> - Transfers to a specific email recipient.</li>
   * <li><code>?transferId=transfer_...</code> - Look up a single transfer by ID; bypasses pagination.</li>
   * </ul>
   */
  public CdpClientHttpResponse<ListTransfersResponse> listTransfers() {
    return listTransfers(ListTransfersRequest.builder().build());
  }

  /**
   * List transfers for your organization. Use this to view and monitor your transfer activity.
   * <p><strong>Status Filtering</strong>: Filter by specific status to efficiently manage transfers:</p>
   * <ul>
   * <li><code>?status=processing</code> - Monitor active transfers.</li>
   * <li><code>?status=quoted</code> - Find transfers awaiting execution.</li>
   * <li><code>?status=failed</code> - Review failed transfers for troubleshooting.</li>
   * <li><code>?status=completed</code> - Find completed transfers.</li>
   * </ul>
   * <p><strong>Account Filtering</strong>: Filter by account ID to find transfers involving a specific account:</p>
   * <ul>
   * <li><code>?accountId=&lt;ID&gt;</code> - All transfers where the account is either source or target (OR semantics).</li>
   * <li><code>?sourceAccountId=&lt;ID&gt;</code> - Only transfers where the account is the source (outbound).</li>
   * <li><code>?targetAccountId=&lt;ID&gt;</code> - Only transfers where the account is the target (inbound).
   * Providing <code>accountId</code> together with <code>sourceAccountId</code> or <code>targetAccountId</code> is a validation error and returns HTTP 400.</li>
   * </ul>
   * <p><strong>Date Range Filtering</strong>: Filter by creation or last-updated time for reconciliation:</p>
   * <ul>
   * <li><code>?createdAfter=2026-01-01T00:00:00Z&amp;createdBefore=2026-01-31T23:59:59Z</code> - Transfers created within a date range.</li>
   * <li><code>?updatedAfter=2026-01-01T00:00:00Z</code> - Transfers updated since a given time. Useful for incremental sync.</li>
   * </ul>
   * <p><strong>Asset Filtering</strong>: Filter by source or target asset symbol:</p>
   * <ul>
   * <li><code>?sourceAsset=usd</code> - Transfers funded from a USD account.</li>
   * <li><code>?targetAsset=usdc</code> - Transfers delivering USDC to the target.
   * Supported asset symbols include <code>usdc</code>, <code>eurc</code>, <code>usd</code>, and <code>eur</code>.</li>
   * </ul>
   * <p><strong>Other Filters</strong>:</p>
   * <ul>
   * <li><code>?sourceAddress=0x...</code> - Transfers from a specific on-chain source address.</li>
   * <li><code>?targetAddress=0x...</code> - Transfers to a specific on-chain destination address.</li>
   * <li><code>?targetEmail=user@example.com</code> - Transfers to a specific email recipient.</li>
   * <li><code>?transferId=transfer_...</code> - Look up a single transfer by ID; bypasses pagination.</li>
   * </ul>
   */
  public CdpClientHttpResponse<ListTransfersResponse> listTransfers(RequestOptions requestOptions) {
    return listTransfers(ListTransfersRequest.builder().build(),requestOptions);
  }

  /**
   * List transfers for your organization. Use this to view and monitor your transfer activity.
   * <p><strong>Status Filtering</strong>: Filter by specific status to efficiently manage transfers:</p>
   * <ul>
   * <li><code>?status=processing</code> - Monitor active transfers.</li>
   * <li><code>?status=quoted</code> - Find transfers awaiting execution.</li>
   * <li><code>?status=failed</code> - Review failed transfers for troubleshooting.</li>
   * <li><code>?status=completed</code> - Find completed transfers.</li>
   * </ul>
   * <p><strong>Account Filtering</strong>: Filter by account ID to find transfers involving a specific account:</p>
   * <ul>
   * <li><code>?accountId=&lt;ID&gt;</code> - All transfers where the account is either source or target (OR semantics).</li>
   * <li><code>?sourceAccountId=&lt;ID&gt;</code> - Only transfers where the account is the source (outbound).</li>
   * <li><code>?targetAccountId=&lt;ID&gt;</code> - Only transfers where the account is the target (inbound).
   * Providing <code>accountId</code> together with <code>sourceAccountId</code> or <code>targetAccountId</code> is a validation error and returns HTTP 400.</li>
   * </ul>
   * <p><strong>Date Range Filtering</strong>: Filter by creation or last-updated time for reconciliation:</p>
   * <ul>
   * <li><code>?createdAfter=2026-01-01T00:00:00Z&amp;createdBefore=2026-01-31T23:59:59Z</code> - Transfers created within a date range.</li>
   * <li><code>?updatedAfter=2026-01-01T00:00:00Z</code> - Transfers updated since a given time. Useful for incremental sync.</li>
   * </ul>
   * <p><strong>Asset Filtering</strong>: Filter by source or target asset symbol:</p>
   * <ul>
   * <li><code>?sourceAsset=usd</code> - Transfers funded from a USD account.</li>
   * <li><code>?targetAsset=usdc</code> - Transfers delivering USDC to the target.
   * Supported asset symbols include <code>usdc</code>, <code>eurc</code>, <code>usd</code>, and <code>eur</code>.</li>
   * </ul>
   * <p><strong>Other Filters</strong>:</p>
   * <ul>
   * <li><code>?sourceAddress=0x...</code> - Transfers from a specific on-chain source address.</li>
   * <li><code>?targetAddress=0x...</code> - Transfers to a specific on-chain destination address.</li>
   * <li><code>?targetEmail=user@example.com</code> - Transfers to a specific email recipient.</li>
   * <li><code>?transferId=transfer_...</code> - Look up a single transfer by ID; bypasses pagination.</li>
   * </ul>
   */
  public CdpClientHttpResponse<ListTransfersResponse> listTransfers(ListTransfersRequest request) {
    return listTransfers(request,null);
  }

  /**
   * List transfers for your organization. Use this to view and monitor your transfer activity.
   * <p><strong>Status Filtering</strong>: Filter by specific status to efficiently manage transfers:</p>
   * <ul>
   * <li><code>?status=processing</code> - Monitor active transfers.</li>
   * <li><code>?status=quoted</code> - Find transfers awaiting execution.</li>
   * <li><code>?status=failed</code> - Review failed transfers for troubleshooting.</li>
   * <li><code>?status=completed</code> - Find completed transfers.</li>
   * </ul>
   * <p><strong>Account Filtering</strong>: Filter by account ID to find transfers involving a specific account:</p>
   * <ul>
   * <li><code>?accountId=&lt;ID&gt;</code> - All transfers where the account is either source or target (OR semantics).</li>
   * <li><code>?sourceAccountId=&lt;ID&gt;</code> - Only transfers where the account is the source (outbound).</li>
   * <li><code>?targetAccountId=&lt;ID&gt;</code> - Only transfers where the account is the target (inbound).
   * Providing <code>accountId</code> together with <code>sourceAccountId</code> or <code>targetAccountId</code> is a validation error and returns HTTP 400.</li>
   * </ul>
   * <p><strong>Date Range Filtering</strong>: Filter by creation or last-updated time for reconciliation:</p>
   * <ul>
   * <li><code>?createdAfter=2026-01-01T00:00:00Z&amp;createdBefore=2026-01-31T23:59:59Z</code> - Transfers created within a date range.</li>
   * <li><code>?updatedAfter=2026-01-01T00:00:00Z</code> - Transfers updated since a given time. Useful for incremental sync.</li>
   * </ul>
   * <p><strong>Asset Filtering</strong>: Filter by source or target asset symbol:</p>
   * <ul>
   * <li><code>?sourceAsset=usd</code> - Transfers funded from a USD account.</li>
   * <li><code>?targetAsset=usdc</code> - Transfers delivering USDC to the target.
   * Supported asset symbols include <code>usdc</code>, <code>eurc</code>, <code>usd</code>, and <code>eur</code>.</li>
   * </ul>
   * <p><strong>Other Filters</strong>:</p>
   * <ul>
   * <li><code>?sourceAddress=0x...</code> - Transfers from a specific on-chain source address.</li>
   * <li><code>?targetAddress=0x...</code> - Transfers to a specific on-chain destination address.</li>
   * <li><code>?targetEmail=user@example.com</code> - Transfers to a specific email recipient.</li>
   * <li><code>?transferId=transfer_...</code> - Look up a single transfer by ID; bypasses pagination.</li>
   * </ul>
   */
  public CdpClientHttpResponse<ListTransfersResponse> listTransfers(ListTransfersRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v2/transfers");if (request.getStatus().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "status", request.getStatus().get(), false);
      }
      if (request.getAccountId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "accountId", request.getAccountId().get(), false);
      }
      if (request.getSourceAccountId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sourceAccountId", request.getSourceAccountId().get(), false);
      }
      if (request.getTargetAccountId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "targetAccountId", request.getTargetAccountId().get(), false);
      }
      if (request.getCreatedAfter().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "createdAfter", request.getCreatedAfter().get(), false);
      }
      if (request.getCreatedBefore().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "createdBefore", request.getCreatedBefore().get(), false);
      }
      if (request.getUpdatedAfter().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "updatedAfter", request.getUpdatedAfter().get(), false);
      }
      if (request.getUpdatedBefore().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "updatedBefore", request.getUpdatedBefore().get(), false);
      }
      if (request.getSourceAsset().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sourceAsset", request.getSourceAsset().get(), false);
      }
      if (request.getTargetAsset().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "targetAsset", request.getTargetAsset().get(), false);
      }
      if (request.getSourceAddress().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "sourceAddress", request.getSourceAddress().get(), false);
      }
      if (request.getTargetAddress().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "targetAddress", request.getTargetAddress().get(), false);
      }
      if (request.getTargetEmail().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "targetEmail", request.getTargetEmail().get(), false);
      }
      if (request.getTransferId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "transferId", request.getTransferId().get(), false);
      }
      if (request.getPageSize().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageSize", request.getPageSize().get(), false);
      }
      if (request.getPageToken().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageToken", request.getPageToken().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListTransfersResponse.class), response);
        }
        try {
          if (response.code() == 400) {
            throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new CdpClientException("Failed to decode HTTP response", e);
      }
      catch (IOException e) {
        throw new CdpClientException("Network error executing HTTP request", e);
      }
    }

    /**
     * Create a new transfer to move funds from a source to a target.
     * All transfers first transition to <code>quoted</code>. If <code>execute: false</code>, the transfer stays quoted until you call <code>/v2/transfers/{transferId}/execute</code>.
     * If <code>execute: true</code>, quoted status emits momentarily before the transfer moves to <code>processing</code>, where execution proceeds. Subscribe to the transfers webhook to  follow progress in real time instead of polling.
     */
    public CdpClientHttpResponse<Transfer> createTransfer(TransferRequest request) {
      return createTransfer(request,null);
    }

    /**
     * Create a new transfer to move funds from a source to a target.
     * All transfers first transition to <code>quoted</code>. If <code>execute: false</code>, the transfer stays quoted until you call <code>/v2/transfers/{transferId}/execute</code>.
     * If <code>execute: true</code>, quoted status emits momentarily before the transfer moves to <code>processing</code>, where execution proceeds. Subscribe to the transfers webhook to  follow progress in real time instead of polling.
     */
    public CdpClientHttpResponse<Transfer> createTransfer(TransferRequest request,
        RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v2/transfers");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
          _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Transfer.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new CdpClientException("Failed to decode HTTP response", e);
        }
        catch (IOException e) {
          throw new CdpClientException("Network error executing HTTP request", e);
        }
      }

      /**
       * Get a transfer by its ID.
       */
      public CdpClientHttpResponse<Transfer> getTransferById(String transferId) {
        return getTransferById(transferId,GetTransferByIdRequest.builder().build());
      }

      /**
       * Get a transfer by its ID.
       */
      public CdpClientHttpResponse<Transfer> getTransferById(String transferId,
          RequestOptions requestOptions) {
        return getTransferById(transferId,GetTransferByIdRequest.builder().build(),requestOptions);
      }

      /**
       * Get a transfer by its ID.
       */
      public CdpClientHttpResponse<Transfer> getTransferById(String transferId,
          GetTransferByIdRequest request) {
        return getTransferById(transferId,request,null);
      }

      /**
       * Get a transfer by its ID.
       */
      public CdpClientHttpResponse<Transfer> getTransferById(String transferId,
          GetTransferByIdRequest request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v2/transfers")
          .addPathSegment(transferId);if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Transfer.class), response);
            }
            try {
              if (response.code() == 404) {
                throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (JsonProcessingException e) {
            throw new CdpClientException("Failed to decode HTTP response", e);
          }
          catch (IOException e) {
            throw new CdpClientException("Network error executing HTTP request", e);
          }
        }

        /**
         * Executes a transfer which was created using the Create a transfer endpoint.
         */
        public CdpClientHttpResponse<Transfer> executeFundTransfer(String transferId) {
          return executeFundTransfer(transferId,ExecuteFundTransferRequest.builder().build());
        }

        /**
         * Executes a transfer which was created using the Create a transfer endpoint.
         */
        public CdpClientHttpResponse<Transfer> executeFundTransfer(String transferId,
            RequestOptions requestOptions) {
          return executeFundTransfer(transferId,ExecuteFundTransferRequest.builder().build(),requestOptions);
        }

        /**
         * Executes a transfer which was created using the Create a transfer endpoint.
         */
        public CdpClientHttpResponse<Transfer> executeFundTransfer(String transferId,
            ExecuteFundTransferRequest request) {
          return executeFundTransfer(transferId,request,null);
        }

        /**
         * Executes a transfer which was created using the Create a transfer endpoint.
         */
        public CdpClientHttpResponse<Transfer> executeFundTransfer(String transferId,
            ExecuteFundTransferRequest request, RequestOptions requestOptions) {
          HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

            .addPathSegments("v2/transfers")
            .addPathSegment(transferId)
            .addPathSegments("execute");if (requestOptions != null) {
              requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
              } );
            }
            Request.Builder _requestBuilder = new Request.Builder()
              .url(httpUrl.build())
              .method("POST", RequestBody.create("", null))
              .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
              .headers(Headers.of(clientOptions.headers(requestOptions)))
              .addHeader("Accept", "application/json");
            if (request.getIdempotencyKey().isPresent()) {
              _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
            }
            Request okhttpRequest = _requestBuilder.build();
            OkHttpClient client = clientOptions.httpClient();
            if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
              client = clientOptions.httpClientWithTimeout(requestOptions);
            }
            try (Response response = client.newCall(okhttpRequest).execute()) {
              ResponseBody responseBody = response.body();
              String responseBodyString = responseBody != null ? responseBody.string() : "{}";
              if (response.isSuccessful()) {
                return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Transfer.class), response);
              }
              try {
                switch (response.code()) {
                  case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 429:throw new TooManyRequestsError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                }
              }
              catch (JsonProcessingException ignored) {
                // unable to map error response, throwing generic error
              }
              Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
              throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
            }
            catch (JsonProcessingException e) {
              throw new CdpClientException("Failed to decode HTTP response", e);
            }
            catch (IOException e) {
              throw new CdpClientException("Network error executing HTTP request", e);
            }
          }

          /**
           * Submit travel rule information for a deposit transfer held pending compliance review.
           * <p>Required fields vary by jurisdiction and may include originator name, address, date of birth, personal ID, and VASP information.</p>
           * <p>If the submitted information satisfies all jurisdictional requirements, <code>status</code> will be <code>completed</code> and the transfer will proceed. Otherwise, <code>status</code> will be <code>incomplete</code> and <code>missingFields</code> will indicate which fields still need to be provided.</p>
           */
          public CdpClientHttpResponse<DepositTravelRuleResponse> submitDepositTravelRule(
              String transferId) {
            return submitDepositTravelRule(transferId,DepositTravelRuleRequest.builder().build());
          }

          /**
           * Submit travel rule information for a deposit transfer held pending compliance review.
           * <p>Required fields vary by jurisdiction and may include originator name, address, date of birth, personal ID, and VASP information.</p>
           * <p>If the submitted information satisfies all jurisdictional requirements, <code>status</code> will be <code>completed</code> and the transfer will proceed. Otherwise, <code>status</code> will be <code>incomplete</code> and <code>missingFields</code> will indicate which fields still need to be provided.</p>
           */
          public CdpClientHttpResponse<DepositTravelRuleResponse> submitDepositTravelRule(
              String transferId, RequestOptions requestOptions) {
            return submitDepositTravelRule(transferId,DepositTravelRuleRequest.builder().build(),requestOptions);
          }

          /**
           * Submit travel rule information for a deposit transfer held pending compliance review.
           * <p>Required fields vary by jurisdiction and may include originator name, address, date of birth, personal ID, and VASP information.</p>
           * <p>If the submitted information satisfies all jurisdictional requirements, <code>status</code> will be <code>completed</code> and the transfer will proceed. Otherwise, <code>status</code> will be <code>incomplete</code> and <code>missingFields</code> will indicate which fields still need to be provided.</p>
           */
          public CdpClientHttpResponse<DepositTravelRuleResponse> submitDepositTravelRule(
              String transferId, DepositTravelRuleRequest request) {
            return submitDepositTravelRule(transferId,request,null);
          }

          /**
           * Submit travel rule information for a deposit transfer held pending compliance review.
           * <p>Required fields vary by jurisdiction and may include originator name, address, date of birth, personal ID, and VASP information.</p>
           * <p>If the submitted information satisfies all jurisdictional requirements, <code>status</code> will be <code>completed</code> and the transfer will proceed. Otherwise, <code>status</code> will be <code>incomplete</code> and <code>missingFields</code> will indicate which fields still need to be provided.</p>
           */
          public CdpClientHttpResponse<DepositTravelRuleResponse> submitDepositTravelRule(
              String transferId, DepositTravelRuleRequest request, RequestOptions requestOptions) {
            HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

              .addPathSegments("v2/transfers")
              .addPathSegment(transferId)
              .addPathSegments("travel-rule");if (requestOptions != null) {
                requestOptions.getQueryParameters().forEach((_key, _value) -> {
                  httpUrl.addQueryParameter(_key, _value);
                } );
              }
              RequestBody body;
              try {
                body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
              }
              catch(Exception e) {
                throw new RuntimeException(e);
              }
              Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json");
              if (request.getIdempotencyKey().isPresent()) {
                _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
              }
              Request okhttpRequest = _requestBuilder.build();
              OkHttpClient client = clientOptions.httpClient();
              if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
                client = clientOptions.httpClientWithTimeout(requestOptions);
              }
              try (Response response = client.newCall(okhttpRequest).execute()) {
                ResponseBody responseBody = response.body();
                String responseBodyString = responseBody != null ? responseBody.string() : "{}";
                if (response.isSuccessful()) {
                  return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DepositTravelRuleResponse.class), response);
                }
                try {
                  switch (response.code()) {
                    case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                    case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                  }
                }
                catch (JsonProcessingException ignored) {
                  // unable to map error response, throwing generic error
                }
                Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
                throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
              }
              catch (JsonProcessingException e) {
                throw new CdpClientException("Failed to decode HTTP response", e);
              }
              catch (IOException e) {
                throw new CdpClientException("Network error executing HTTP request", e);
              }
            }
          }
