/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.resources.enduseraccountmanagement;

import com.coinbase.cdp.core.ClientOptions;
import com.coinbase.cdp.core.RequestOptions;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.AddEndUserEvmAccountRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.AddEndUserEvmSmartAccountRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.AddEndUserSolanaAccountRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.CreateEndUserRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.GetEndUserRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.ImportEndUserRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.ListEndUsersRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.LookupEndUserRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.requests.ValidateEndUserAccessTokenRequest;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.AddEndUserEvmAccountResponse;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.AddEndUserEvmSmartAccountResponse;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.AddEndUserSolanaAccountResponse;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.ListEndUsersResponse;
import com.coinbase.cdp.resources.enduseraccountmanagement.types.LookupEndUserResponse;
import com.coinbase.cdp.types.EndUser;
import java.lang.Object;
import java.lang.String;
import java.util.Map;
import java.util.concurrent.CompletableFuture;

public class AsyncEndUserAccountManagementClient {
  protected final ClientOptions clientOptions;

  private final AsyncWithRawResponseEndUserAccountManagementClient rawClient;

  public AsyncEndUserAccountManagementClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
    this.rawClient = new AsyncWithRawResponseEndUserAccountManagementClient(clientOptions);
  }

  /**
   * Get responses with HTTP metadata like headers
   */
  public AsyncWithRawResponseEndUserAccountManagementClient withRawResponse() {
    return this.rawClient;
  }

  /**
   * Lists the end users belonging to the developer's CDP Project.
   * By default, the response is sorted by creation date in ascending order and paginated to 20 users per page.
   */
  public CompletableFuture<ListEndUsersResponse> listEndUsers() {
    return this.rawClient.listEndUsers().thenApply(response -> response.body());
  }

  /**
   * Lists the end users belonging to the developer's CDP Project.
   * By default, the response is sorted by creation date in ascending order and paginated to 20 users per page.
   */
  public CompletableFuture<ListEndUsersResponse> listEndUsers(RequestOptions requestOptions) {
    return this.rawClient.listEndUsers(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Lists the end users belonging to the developer's CDP Project.
   * By default, the response is sorted by creation date in ascending order and paginated to 20 users per page.
   */
  public CompletableFuture<ListEndUsersResponse> listEndUsers(ListEndUsersRequest request) {
    return this.rawClient.listEndUsers(request).thenApply(response -> response.body());
  }

  /**
   * Lists the end users belonging to the developer's CDP Project.
   * By default, the response is sorted by creation date in ascending order and paginated to 20 users per page.
   */
  public CompletableFuture<ListEndUsersResponse> listEndUsers(ListEndUsersRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.listEndUsers(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates an end user. An end user is an entity that can own CDP EVM accounts, EVM smart accounts, and/or Solana accounts. 1 or more authentication methods must be associated with an end user. By default, no accounts are created unless the optional <code>evmAccount</code> and/or <code>solanaAccount</code> fields are provided.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<EndUser> createEndUser(CreateEndUserRequest request) {
    return this.rawClient.createEndUser(request).thenApply(response -> response.body());
  }

  /**
   * Creates an end user. An end user is an entity that can own CDP EVM accounts, EVM smart accounts, and/or Solana accounts. 1 or more authentication methods must be associated with an end user. By default, no accounts are created unless the optional <code>evmAccount</code> and/or <code>solanaAccount</code> fields are provided.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<EndUser> createEndUser(CreateEndUserRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.createEndUser(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Validates the end user's access token and returns the end user's information. Returns an error if the access token is invalid or expired.
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<EndUser> validateEndUserAccessToken(
      ValidateEndUserAccessTokenRequest request) {
    return this.rawClient.validateEndUserAccessToken(request).thenApply(response -> response.body());
  }

  /**
   * Validates the end user's access token and returns the end user's information. Returns an error if the access token is invalid or expired.
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<EndUser> validateEndUserAccessToken(
      ValidateEndUserAccessTokenRequest request, RequestOptions requestOptions) {
    return this.rawClient.validateEndUserAccessToken(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Gets an end user by ID.
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<EndUser> getEndUser(String userId) {
    return this.rawClient.getEndUser(userId).thenApply(response -> response.body());
  }

  /**
   * Gets an end user by ID.
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<EndUser> getEndUser(String userId, RequestOptions requestOptions) {
    return this.rawClient.getEndUser(userId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Gets an end user by ID.
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<EndUser> getEndUser(String userId, GetEndUserRequest request) {
    return this.rawClient.getEndUser(userId, request).thenApply(response -> response.body());
  }

  /**
   * Gets an end user by ID.
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<EndUser> getEndUser(String userId, GetEndUserRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.getEndUser(userId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Looks up end users. Exactly one lookup type must be provided per request:
   * <ul>
   * <li>
   * <p><strong>email</strong>: searches across all email-based authentication methods
   * (email, Google, Apple, GitHub). May return multiple end users if the
   * same email address appears across different auth methods.</p>
   * </li>
   * <li>
   * <p><strong>oauthProvider + oauthSubject</strong>: looks up a user by their OAuth
   * provider and subject (the <code>sub</code> claim from the provider's ID token).
   * Both params must be provided together.</p>
   * </li>
   * <li>
   * <p><strong>phoneNumber</strong>: looks up a user by their SMS-verified phone number.</p>
   * </li>
   * <li>
   * <p><strong>siweAddress</strong>: looks up a user by the Ethereum address they authenticated
   * with via Sign In With Ethereum (EIP-4361).</p>
   * </li>
   * </ul>
   * <p>Returns all matching end users. If no end users match, an empty array is returned.</p>
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<LookupEndUserResponse> lookupEndUser() {
    return this.rawClient.lookupEndUser().thenApply(response -> response.body());
  }

  /**
   * Looks up end users. Exactly one lookup type must be provided per request:
   * <ul>
   * <li>
   * <p><strong>email</strong>: searches across all email-based authentication methods
   * (email, Google, Apple, GitHub). May return multiple end users if the
   * same email address appears across different auth methods.</p>
   * </li>
   * <li>
   * <p><strong>oauthProvider + oauthSubject</strong>: looks up a user by their OAuth
   * provider and subject (the <code>sub</code> claim from the provider's ID token).
   * Both params must be provided together.</p>
   * </li>
   * <li>
   * <p><strong>phoneNumber</strong>: looks up a user by their SMS-verified phone number.</p>
   * </li>
   * <li>
   * <p><strong>siweAddress</strong>: looks up a user by the Ethereum address they authenticated
   * with via Sign In With Ethereum (EIP-4361).</p>
   * </li>
   * </ul>
   * <p>Returns all matching end users. If no end users match, an empty array is returned.</p>
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<LookupEndUserResponse> lookupEndUser(RequestOptions requestOptions) {
    return this.rawClient.lookupEndUser(requestOptions).thenApply(response -> response.body());
  }

  /**
   * Looks up end users. Exactly one lookup type must be provided per request:
   * <ul>
   * <li>
   * <p><strong>email</strong>: searches across all email-based authentication methods
   * (email, Google, Apple, GitHub). May return multiple end users if the
   * same email address appears across different auth methods.</p>
   * </li>
   * <li>
   * <p><strong>oauthProvider + oauthSubject</strong>: looks up a user by their OAuth
   * provider and subject (the <code>sub</code> claim from the provider's ID token).
   * Both params must be provided together.</p>
   * </li>
   * <li>
   * <p><strong>phoneNumber</strong>: looks up a user by their SMS-verified phone number.</p>
   * </li>
   * <li>
   * <p><strong>siweAddress</strong>: looks up a user by the Ethereum address they authenticated
   * with via Sign In With Ethereum (EIP-4361).</p>
   * </li>
   * </ul>
   * <p>Returns all matching end users. If no end users match, an empty array is returned.</p>
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<LookupEndUserResponse> lookupEndUser(LookupEndUserRequest request) {
    return this.rawClient.lookupEndUser(request).thenApply(response -> response.body());
  }

  /**
   * Looks up end users. Exactly one lookup type must be provided per request:
   * <ul>
   * <li>
   * <p><strong>email</strong>: searches across all email-based authentication methods
   * (email, Google, Apple, GitHub). May return multiple end users if the
   * same email address appears across different auth methods.</p>
   * </li>
   * <li>
   * <p><strong>oauthProvider + oauthSubject</strong>: looks up a user by their OAuth
   * provider and subject (the <code>sub</code> claim from the provider's ID token).
   * Both params must be provided together.</p>
   * </li>
   * <li>
   * <p><strong>phoneNumber</strong>: looks up a user by their SMS-verified phone number.</p>
   * </li>
   * <li>
   * <p><strong>siweAddress</strong>: looks up a user by the Ethereum address they authenticated
   * with via Sign In With Ethereum (EIP-4361).</p>
   * </li>
   * </ul>
   * <p>Returns all matching end users. If no end users match, an empty array is returned.</p>
   * <p>This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.</p>
   */
  public CompletableFuture<LookupEndUserResponse> lookupEndUser(LookupEndUserRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.lookupEndUser(request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Adds a new EVM EOA account to an existing end user. End users can have up to 10 EVM accounts.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserEvmAccountResponse> addEndUserEvmAccount(String userId,
      Map<String, Object> body) {
    return this.rawClient.addEndUserEvmAccount(userId, body).thenApply(response -> response.body());
  }

  /**
   * Adds a new EVM EOA account to an existing end user. End users can have up to 10 EVM accounts.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserEvmAccountResponse> addEndUserEvmAccount(String userId,
      Map<String, Object> body, RequestOptions requestOptions) {
    return this.rawClient.addEndUserEvmAccount(userId, body, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Adds a new EVM EOA account to an existing end user. End users can have up to 10 EVM accounts.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserEvmAccountResponse> addEndUserEvmAccount(String userId,
      AddEndUserEvmAccountRequest request) {
    return this.rawClient.addEndUserEvmAccount(userId, request).thenApply(response -> response.body());
  }

  /**
   * Adds a new EVM EOA account to an existing end user. End users can have up to 10 EVM accounts.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserEvmAccountResponse> addEndUserEvmAccount(String userId,
      AddEndUserEvmAccountRequest request, RequestOptions requestOptions) {
    return this.rawClient.addEndUserEvmAccount(userId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates an EVM smart account for an existing end user. The backend will create a new EVM EOA account to serve as the owner of the smart account.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserEvmSmartAccountResponse> addEndUserEvmSmartAccount(
      String userId) {
    return this.rawClient.addEndUserEvmSmartAccount(userId).thenApply(response -> response.body());
  }

  /**
   * Creates an EVM smart account for an existing end user. The backend will create a new EVM EOA account to serve as the owner of the smart account.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserEvmSmartAccountResponse> addEndUserEvmSmartAccount(
      String userId, RequestOptions requestOptions) {
    return this.rawClient.addEndUserEvmSmartAccount(userId, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Creates an EVM smart account for an existing end user. The backend will create a new EVM EOA account to serve as the owner of the smart account.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserEvmSmartAccountResponse> addEndUserEvmSmartAccount(
      String userId, AddEndUserEvmSmartAccountRequest request) {
    return this.rawClient.addEndUserEvmSmartAccount(userId, request).thenApply(response -> response.body());
  }

  /**
   * Creates an EVM smart account for an existing end user. The backend will create a new EVM EOA account to serve as the owner of the smart account.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserEvmSmartAccountResponse> addEndUserEvmSmartAccount(
      String userId, AddEndUserEvmSmartAccountRequest request, RequestOptions requestOptions) {
    return this.rawClient.addEndUserEvmSmartAccount(userId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Adds a new Solana account to an existing end user. End users can have up to 10 Solana accounts.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserSolanaAccountResponse> addEndUserSolanaAccount(String userId,
      Map<String, Object> body) {
    return this.rawClient.addEndUserSolanaAccount(userId, body).thenApply(response -> response.body());
  }

  /**
   * Adds a new Solana account to an existing end user. End users can have up to 10 Solana accounts.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserSolanaAccountResponse> addEndUserSolanaAccount(String userId,
      Map<String, Object> body, RequestOptions requestOptions) {
    return this.rawClient.addEndUserSolanaAccount(userId, body, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Adds a new Solana account to an existing end user. End users can have up to 10 Solana accounts.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserSolanaAccountResponse> addEndUserSolanaAccount(String userId,
      AddEndUserSolanaAccountRequest request) {
    return this.rawClient.addEndUserSolanaAccount(userId, request).thenApply(response -> response.body());
  }

  /**
   * Adds a new Solana account to an existing end user. End users can have up to 10 Solana accounts.
   * This API is intended to be used by the developer's own backend, and is authenticated using the developer's CDP API key.
   */
  public CompletableFuture<AddEndUserSolanaAccountResponse> addEndUserSolanaAccount(String userId,
      AddEndUserSolanaAccountRequest request, RequestOptions requestOptions) {
    return this.rawClient.addEndUserSolanaAccount(userId, request, requestOptions).thenApply(response -> response.body());
  }

  /**
   * Imports an existing private key for an end user into the developer's CDP Project. The private key must be encrypted using the CDP SDK's encryption scheme before being sent to this endpoint. This API should be called from the <a href="https://github.com/coinbase/cdp-sdk">CDP SDK</a> to ensure that the associated private key is properly encrypted.
   * <p>This endpoint allows developers to import existing keys for their end users, supporting both EVM and Solana key types. The end user must have at least one authentication method configured.</p>
   */
  public CompletableFuture<EndUser> importEndUser(ImportEndUserRequest request) {
    return this.rawClient.importEndUser(request).thenApply(response -> response.body());
  }

  /**
   * Imports an existing private key for an end user into the developer's CDP Project. The private key must be encrypted using the CDP SDK's encryption scheme before being sent to this endpoint. This API should be called from the <a href="https://github.com/coinbase/cdp-sdk">CDP SDK</a> to ensure that the associated private key is properly encrypted.
   * <p>This endpoint allows developers to import existing keys for their end users, supporting both EVM and Solana key types. The end user must have at least one authentication method configured.</p>
   */
  public CompletableFuture<EndUser> importEndUser(ImportEndUserRequest request,
      RequestOptions requestOptions) {
    return this.rawClient.importEndUser(request, requestOptions).thenApply(response -> response.body());
  }
}
