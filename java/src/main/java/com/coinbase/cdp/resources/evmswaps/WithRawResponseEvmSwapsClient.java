/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.resources.evmswaps;

import com.coinbase.cdp.core.CdpClientApiException;
import com.coinbase.cdp.core.CdpClientException;
import com.coinbase.cdp.core.CdpClientHttpResponse;
import com.coinbase.cdp.core.ClientOptions;
import com.coinbase.cdp.core.EndpointMetadata;
import com.coinbase.cdp.core.MediaTypes;
import com.coinbase.cdp.core.ObjectMappers;
import com.coinbase.cdp.core.QueryStringMapper;
import com.coinbase.cdp.core.RequestOptions;
import com.coinbase.cdp.errors.BadGatewayError;
import com.coinbase.cdp.errors.BadRequestError;
import com.coinbase.cdp.errors.ForbiddenError;
import com.coinbase.cdp.errors.InternalServerError;
import com.coinbase.cdp.errors.ServiceUnavailableError;
import com.coinbase.cdp.resources.evmswaps.requests.CreateEvmSwapQuoteRequest;
import com.coinbase.cdp.resources.evmswaps.requests.GetEvmSwapPriceRequest;
import com.coinbase.cdp.types.CreateSwapQuoteResponseWrapper;
import com.coinbase.cdp.types.Error;
import com.coinbase.cdp.types.GetSwapPriceResponseWrapper;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.Collections;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class WithRawResponseEvmSwapsClient {
  protected final ClientOptions clientOptions;

  WithRawResponseEvmSwapsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Get a price estimate for a swap between two tokens on an EVM network.
   */
  public CdpClientHttpResponse<GetSwapPriceResponseWrapper> getEvmSwapPrice(
      GetEvmSwapPriceRequest request) {
    return getEvmSwapPrice(request,null);
  }

  /**
   * Get a price estimate for a swap between two tokens on an EVM network.
   */
  public CdpClientHttpResponse<GetSwapPriceResponseWrapper> getEvmSwapPrice(
      GetEvmSwapPriceRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v2/evm/swaps/quote");QueryStringMapper.addQueryParameter(httpUrl, "network", request.getNetwork(), false);
      QueryStringMapper.addQueryParameter(httpUrl, "toToken", request.getToToken(), false);
      QueryStringMapper.addQueryParameter(httpUrl, "fromToken", request.getFromToken(), false);
      QueryStringMapper.addQueryParameter(httpUrl, "fromAmount", request.getFromAmount(), false);
      QueryStringMapper.addQueryParameter(httpUrl, "taker", request.getTaker(), false);
      if (request.getSignerAddress().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "signerAddress", request.getSignerAddress().get(), false);
      }
      if (request.getGasPrice().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "gasPrice", request.getGasPrice().get(), false);
      }
      if (request.getSlippageBps().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "slippageBps", request.getSlippageBps().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, GetSwapPriceResponseWrapper.class), response);
        }
        try {
          switch (response.code()) {
            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new CdpClientException("Failed to decode HTTP response", e);
      }
      catch (IOException e) {
        throw new CdpClientException("Network error executing HTTP request", e);
      }
    }

    /**
     * Create a swap quote, which includes the payload to sign as well as the transaction data needed to execute the swap. The developer is responsible for signing the payload and submitting the transaction to the network in order to execute the swap.
     */
    public CdpClientHttpResponse<CreateSwapQuoteResponseWrapper> createEvmSwapQuote(
        CreateEvmSwapQuoteRequest request) {
      return createEvmSwapQuote(request,null);
    }

    /**
     * Create a swap quote, which includes the payload to sign as well as the transaction data needed to execute the swap. The developer is responsible for signing the payload and submitting the transaction to the network in order to execute the swap.
     */
    public CdpClientHttpResponse<CreateSwapQuoteResponseWrapper> createEvmSwapQuote(
        CreateEvmSwapQuoteRequest request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v2/evm/swaps");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
          _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, CreateSwapQuoteResponseWrapper.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new CdpClientException("Failed to decode HTTP response", e);
        }
        catch (IOException e) {
          throw new CdpClientException("Network error executing HTTP request", e);
        }
      }
    }
