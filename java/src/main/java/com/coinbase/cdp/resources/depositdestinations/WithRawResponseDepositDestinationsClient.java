/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.resources.depositdestinations;

import com.coinbase.cdp.core.CdpClientApiException;
import com.coinbase.cdp.core.CdpClientException;
import com.coinbase.cdp.core.CdpClientHttpResponse;
import com.coinbase.cdp.core.ClientOptions;
import com.coinbase.cdp.core.EndpointMetadata;
import com.coinbase.cdp.core.MediaTypes;
import com.coinbase.cdp.core.ObjectMappers;
import com.coinbase.cdp.core.QueryStringMapper;
import com.coinbase.cdp.core.RequestOptions;
import com.coinbase.cdp.errors.BadRequestError;
import com.coinbase.cdp.errors.ForbiddenError;
import com.coinbase.cdp.errors.InternalServerError;
import com.coinbase.cdp.errors.NotFoundError;
import com.coinbase.cdp.errors.ServiceUnavailableError;
import com.coinbase.cdp.errors.UnauthorizedError;
import com.coinbase.cdp.errors.UnprocessableEntityError;
import com.coinbase.cdp.resources.depositdestinations.requests.CreateDepositDestinationBody;
import com.coinbase.cdp.resources.depositdestinations.requests.GetDepositDestinationByIdRequest;
import com.coinbase.cdp.resources.depositdestinations.requests.ListDepositDestinationsRequest;
import com.coinbase.cdp.resources.depositdestinations.types.ListDepositDestinationsResponse;
import com.coinbase.cdp.types.CreateDepositDestinationRequest;
import com.coinbase.cdp.types.DepositDestination;
import com.coinbase.cdp.types.DepositDestinationId;
import com.coinbase.cdp.types.Error;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Exception;
import java.lang.Object;
import java.lang.RuntimeException;
import java.lang.String;
import java.util.Collections;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class WithRawResponseDepositDestinationsClient {
  protected final ClientOptions clientOptions;

  WithRawResponseDepositDestinationsClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * List deposit destinations. You can optionally filter the results by type, account ID, network, or cryptocurrency address. Results are sorted by creation date in descending order (newest first).
   */
  public CdpClientHttpResponse<ListDepositDestinationsResponse> listDepositDestinations() {
    return listDepositDestinations(ListDepositDestinationsRequest.builder().build());
  }

  /**
   * List deposit destinations. You can optionally filter the results by type, account ID, network, or cryptocurrency address. Results are sorted by creation date in descending order (newest first).
   */
  public CdpClientHttpResponse<ListDepositDestinationsResponse> listDepositDestinations(
      RequestOptions requestOptions) {
    return listDepositDestinations(ListDepositDestinationsRequest.builder().build(),requestOptions);
  }

  /**
   * List deposit destinations. You can optionally filter the results by type, account ID, network, or cryptocurrency address. Results are sorted by creation date in descending order (newest first).
   */
  public CdpClientHttpResponse<ListDepositDestinationsResponse> listDepositDestinations(
      ListDepositDestinationsRequest request) {
    return listDepositDestinations(request,null);
  }

  /**
   * List deposit destinations. You can optionally filter the results by type, account ID, network, or cryptocurrency address. Results are sorted by creation date in descending order (newest first).
   */
  public CdpClientHttpResponse<ListDepositDestinationsResponse> listDepositDestinations(
      ListDepositDestinationsRequest request, RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v2/deposit-destinations");if (request.getAccountId().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "accountId", request.getAccountId().get(), false);
      }
      if (request.getAddress().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "address", request.getAddress().get(), false);
      }
      if (request.getType().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "type", request.getType().get(), false);
      }
      if (request.getNetwork().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "network", request.getNetwork().get(), false);
      }
      if (request.getPageSize().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageSize", request.getPageSize().get(), false);
      }
      if (request.getPageToken().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageToken", request.getPageToken().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListDepositDestinationsResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new CdpClientException("Failed to decode HTTP response", e);
      }
      catch (IOException e) {
        throw new CdpClientException("Network error executing HTTP request", e);
      }
    }

    /**
     * Create a new deposit destination for an account. Two types are supported:
     * <ul>
     * <li>
     * <p><code>crypto</code> (public): provisions a cryptocurrency address on the requested
     * network. The returned <code>crypto.address</code> is the deposit address; funds
     * sent to it are credited to the <code>target</code> account in the specified asset.
     * For Customer-owned accounts, the Customer must have the <code>custodyCrypto</code>
     * and <code>custodyStablecoin</code> capabilities enabled.</p>
     * </li>
     * <li>
     * <p><code>fiat</code> (private-beta): provisions a bank
     * account at a CDP banking partner. The server picks the account type
     * (e.g. <code>us_bank</code>) and supported payment rails based on the account's
     * eligibility; pass <code>fiat.paymentRail</code> to influence partner selection.
     * The returned <code>fiat</code> object contains the bank account details the
     * depositor needs to send funds. For Customer-owned accounts, the Customer
     * must have the <code>custodyFiat</code> capability enabled. Requires account
     * enablement — contact your Coinbase representative for access. Fields
     * and behavior may change before general availability.</p>
     * </li>
     * </ul>
     * <p>For Customer-owned accounts not authorized for the required capabilities,
     * the request is rejected with <code>customer_not_authorized</code> (HTTP 403).</p>
     * <p>The created destination's <code>status</code> starts as <code>pending</code> for fiat (it
     * becomes <code>active</code> once the partner provisioning callback completes) and
     * <code>active</code> for crypto.</p>
     */
    public CdpClientHttpResponse<DepositDestination> createDepositDestination(
        CreateDepositDestinationRequest body) {
      return createDepositDestination(CreateDepositDestinationBody.builder().body(body).build());
    }

    /**
     * Create a new deposit destination for an account. Two types are supported:
     * <ul>
     * <li>
     * <p><code>crypto</code> (public): provisions a cryptocurrency address on the requested
     * network. The returned <code>crypto.address</code> is the deposit address; funds
     * sent to it are credited to the <code>target</code> account in the specified asset.
     * For Customer-owned accounts, the Customer must have the <code>custodyCrypto</code>
     * and <code>custodyStablecoin</code> capabilities enabled.</p>
     * </li>
     * <li>
     * <p><code>fiat</code> (private-beta): provisions a bank
     * account at a CDP banking partner. The server picks the account type
     * (e.g. <code>us_bank</code>) and supported payment rails based on the account's
     * eligibility; pass <code>fiat.paymentRail</code> to influence partner selection.
     * The returned <code>fiat</code> object contains the bank account details the
     * depositor needs to send funds. For Customer-owned accounts, the Customer
     * must have the <code>custodyFiat</code> capability enabled. Requires account
     * enablement — contact your Coinbase representative for access. Fields
     * and behavior may change before general availability.</p>
     * </li>
     * </ul>
     * <p>For Customer-owned accounts not authorized for the required capabilities,
     * the request is rejected with <code>customer_not_authorized</code> (HTTP 403).</p>
     * <p>The created destination's <code>status</code> starts as <code>pending</code> for fiat (it
     * becomes <code>active</code> once the partner provisioning callback completes) and
     * <code>active</code> for crypto.</p>
     */
    public CdpClientHttpResponse<DepositDestination> createDepositDestination(
        CreateDepositDestinationRequest body, RequestOptions requestOptions) {
      return createDepositDestination(CreateDepositDestinationBody.builder().body(body).build(), requestOptions);
    }

    /**
     * Create a new deposit destination for an account. Two types are supported:
     * <ul>
     * <li>
     * <p><code>crypto</code> (public): provisions a cryptocurrency address on the requested
     * network. The returned <code>crypto.address</code> is the deposit address; funds
     * sent to it are credited to the <code>target</code> account in the specified asset.
     * For Customer-owned accounts, the Customer must have the <code>custodyCrypto</code>
     * and <code>custodyStablecoin</code> capabilities enabled.</p>
     * </li>
     * <li>
     * <p><code>fiat</code> (private-beta): provisions a bank
     * account at a CDP banking partner. The server picks the account type
     * (e.g. <code>us_bank</code>) and supported payment rails based on the account's
     * eligibility; pass <code>fiat.paymentRail</code> to influence partner selection.
     * The returned <code>fiat</code> object contains the bank account details the
     * depositor needs to send funds. For Customer-owned accounts, the Customer
     * must have the <code>custodyFiat</code> capability enabled. Requires account
     * enablement — contact your Coinbase representative for access. Fields
     * and behavior may change before general availability.</p>
     * </li>
     * </ul>
     * <p>For Customer-owned accounts not authorized for the required capabilities,
     * the request is rejected with <code>customer_not_authorized</code> (HTTP 403).</p>
     * <p>The created destination's <code>status</code> starts as <code>pending</code> for fiat (it
     * becomes <code>active</code> once the partner provisioning callback completes) and
     * <code>active</code> for crypto.</p>
     */
    public CdpClientHttpResponse<DepositDestination> createDepositDestination(
        CreateDepositDestinationBody request) {
      return createDepositDestination(request,null);
    }

    /**
     * Create a new deposit destination for an account. Two types are supported:
     * <ul>
     * <li>
     * <p><code>crypto</code> (public): provisions a cryptocurrency address on the requested
     * network. The returned <code>crypto.address</code> is the deposit address; funds
     * sent to it are credited to the <code>target</code> account in the specified asset.
     * For Customer-owned accounts, the Customer must have the <code>custodyCrypto</code>
     * and <code>custodyStablecoin</code> capabilities enabled.</p>
     * </li>
     * <li>
     * <p><code>fiat</code> (private-beta): provisions a bank
     * account at a CDP banking partner. The server picks the account type
     * (e.g. <code>us_bank</code>) and supported payment rails based on the account's
     * eligibility; pass <code>fiat.paymentRail</code> to influence partner selection.
     * The returned <code>fiat</code> object contains the bank account details the
     * depositor needs to send funds. For Customer-owned accounts, the Customer
     * must have the <code>custodyFiat</code> capability enabled. Requires account
     * enablement — contact your Coinbase representative for access. Fields
     * and behavior may change before general availability.</p>
     * </li>
     * </ul>
     * <p>For Customer-owned accounts not authorized for the required capabilities,
     * the request is rejected with <code>customer_not_authorized</code> (HTTP 403).</p>
     * <p>The created destination's <code>status</code> starts as <code>pending</code> for fiat (it
     * becomes <code>active</code> once the partner provisioning callback completes) and
     * <code>active</code> for crypto.</p>
     */
    public CdpClientHttpResponse<DepositDestination> createDepositDestination(
        CreateDepositDestinationBody request, RequestOptions requestOptions) {
      HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

        .addPathSegments("v2/deposit-destinations");if (requestOptions != null) {
          requestOptions.getQueryParameters().forEach((_key, _value) -> {
            httpUrl.addQueryParameter(_key, _value);
          } );
        }
        RequestBody body;
        try {
          body = RequestBody.create(ObjectMappers.JSON_MAPPER.writeValueAsBytes(request.getBody()), MediaTypes.APPLICATION_JSON);
        }
        catch(Exception e) {
          throw new RuntimeException(e);
        }
        Request.Builder _requestBuilder = new Request.Builder()
          .url(httpUrl.build())
          .method("POST", body)
          .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
          .headers(Headers.of(clientOptions.headers(requestOptions)))
          .addHeader("Content-Type", "application/json")
          .addHeader("Accept", "application/json");
        if (request.getIdempotencyKey().isPresent()) {
          _requestBuilder.addHeader("X-Idempotency-Key", request.getIdempotencyKey().get());
        }
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
          client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
          ResponseBody responseBody = response.body();
          String responseBodyString = responseBody != null ? responseBody.string() : "{}";
          if (response.isSuccessful()) {
            return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DepositDestination.class), response);
          }
          try {
            switch (response.code()) {
              case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 403:throw new ForbiddenError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 422:throw new UnprocessableEntityError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            }
          }
          catch (JsonProcessingException ignored) {
            // unable to map error response, throwing generic error
          }
          Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
          throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
        }
        catch (JsonProcessingException e) {
          throw new CdpClientException("Failed to decode HTTP response", e);
        }
        catch (IOException e) {
          throw new CdpClientException("Network error executing HTTP request", e);
        }
      }

      /**
       * Get a specific deposit destination by its ID.
       */
      public CdpClientHttpResponse<DepositDestination> getDepositDestinationById(
          DepositDestinationId depositDestinationId) {
        return getDepositDestinationById(depositDestinationId,GetDepositDestinationByIdRequest.builder().build());
      }

      /**
       * Get a specific deposit destination by its ID.
       */
      public CdpClientHttpResponse<DepositDestination> getDepositDestinationById(
          DepositDestinationId depositDestinationId, RequestOptions requestOptions) {
        return getDepositDestinationById(depositDestinationId,GetDepositDestinationByIdRequest.builder().build(),requestOptions);
      }

      /**
       * Get a specific deposit destination by its ID.
       */
      public CdpClientHttpResponse<DepositDestination> getDepositDestinationById(
          DepositDestinationId depositDestinationId, GetDepositDestinationByIdRequest request) {
        return getDepositDestinationById(depositDestinationId,request,null);
      }

      /**
       * Get a specific deposit destination by its ID.
       */
      public CdpClientHttpResponse<DepositDestination> getDepositDestinationById(
          DepositDestinationId depositDestinationId, GetDepositDestinationByIdRequest request,
          RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

          .addPathSegments("v2/deposit-destinations")
          .addPathSegment(depositDestinationId.toString());if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
              httpUrl.addQueryParameter(_key, _value);
            } );
          }
          Request.Builder _requestBuilder = new Request.Builder()
            .url(httpUrl.build())
            .method("GET", null)
            .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
            .headers(Headers.of(clientOptions.headers(requestOptions)))
            .addHeader("Accept", "application/json");
          Request okhttpRequest = _requestBuilder.build();
          OkHttpClient client = clientOptions.httpClient();
          if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
          }
          try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
              return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, DepositDestination.class), response);
            }
            try {
              switch (response.code()) {
                case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                case 401:throw new UnauthorizedError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
                case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
              }
            }
            catch (JsonProcessingException ignored) {
              // unable to map error response, throwing generic error
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
          }
          catch (JsonProcessingException e) {
            throw new CdpClientException("Failed to decode HTTP response", e);
          }
          catch (IOException e) {
            throw new CdpClientException("Network error executing HTTP request", e);
          }
        }
      }
