/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.resources.evmtokenbalances;

import com.coinbase.cdp.core.CdpClientApiException;
import com.coinbase.cdp.core.CdpClientException;
import com.coinbase.cdp.core.CdpClientHttpResponse;
import com.coinbase.cdp.core.ClientOptions;
import com.coinbase.cdp.core.EndpointMetadata;
import com.coinbase.cdp.core.ObjectMappers;
import com.coinbase.cdp.core.QueryStringMapper;
import com.coinbase.cdp.core.RequestOptions;
import com.coinbase.cdp.errors.BadGatewayError;
import com.coinbase.cdp.errors.BadRequestError;
import com.coinbase.cdp.errors.InternalServerError;
import com.coinbase.cdp.errors.NotFoundError;
import com.coinbase.cdp.errors.ServiceUnavailableError;
import com.coinbase.cdp.resources.evmtokenbalances.requests.ListEvmTokenBalancesRequest;
import com.coinbase.cdp.resources.evmtokenbalances.types.ListEvmTokenBalancesResponse;
import com.coinbase.cdp.types.Error;
import com.coinbase.cdp.types.ListEvmTokenBalancesNetwork;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import java.lang.Object;
import java.lang.String;
import java.util.Collections;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class WithRawResponseEvmTokenBalancesClient {
  protected final ClientOptions clientOptions;

  WithRawResponseEvmTokenBalancesClient(ClientOptions clientOptions) {
    this.clientOptions = clientOptions;
  }

  /**
   * Lists the token balances of an EVM address on a given network. The balances include ERC-20 tokens and the native gas token (usually ETH). The response is paginated, and by default, returns 20 balances per page.
   * <p>Calls to the <code>base</code> and <code>base-sepolia</code> networks offer:</p>
   * <ul>
   * <li>99.5% &lt; 2 second freshness from chain tip for new tokens on an address.</li>
   * <li>99.5% &lt; 2 second freshness from chain tip for balance data.</li>
   * </ul>
   * <p>Calls to the <code>ethereum</code> network offer:</p>
   * <ul>
   * <li>99% &lt; 30 second freshness from chain tip for new tokens on an address.</li>
   * <li>99% &lt; 2 second freshness from chain tip for balance data.</li>
   * </ul>
   */
  public CdpClientHttpResponse<ListEvmTokenBalancesResponse> listEvmTokenBalances(
      ListEvmTokenBalancesNetwork network, String address) {
    return listEvmTokenBalances(network,address,ListEvmTokenBalancesRequest.builder().build());
  }

  /**
   * Lists the token balances of an EVM address on a given network. The balances include ERC-20 tokens and the native gas token (usually ETH). The response is paginated, and by default, returns 20 balances per page.
   * <p>Calls to the <code>base</code> and <code>base-sepolia</code> networks offer:</p>
   * <ul>
   * <li>99.5% &lt; 2 second freshness from chain tip for new tokens on an address.</li>
   * <li>99.5% &lt; 2 second freshness from chain tip for balance data.</li>
   * </ul>
   * <p>Calls to the <code>ethereum</code> network offer:</p>
   * <ul>
   * <li>99% &lt; 30 second freshness from chain tip for new tokens on an address.</li>
   * <li>99% &lt; 2 second freshness from chain tip for balance data.</li>
   * </ul>
   */
  public CdpClientHttpResponse<ListEvmTokenBalancesResponse> listEvmTokenBalances(
      ListEvmTokenBalancesNetwork network, String address, RequestOptions requestOptions) {
    return listEvmTokenBalances(network,address,ListEvmTokenBalancesRequest.builder().build(),requestOptions);
  }

  /**
   * Lists the token balances of an EVM address on a given network. The balances include ERC-20 tokens and the native gas token (usually ETH). The response is paginated, and by default, returns 20 balances per page.
   * <p>Calls to the <code>base</code> and <code>base-sepolia</code> networks offer:</p>
   * <ul>
   * <li>99.5% &lt; 2 second freshness from chain tip for new tokens on an address.</li>
   * <li>99.5% &lt; 2 second freshness from chain tip for balance data.</li>
   * </ul>
   * <p>Calls to the <code>ethereum</code> network offer:</p>
   * <ul>
   * <li>99% &lt; 30 second freshness from chain tip for new tokens on an address.</li>
   * <li>99% &lt; 2 second freshness from chain tip for balance data.</li>
   * </ul>
   */
  public CdpClientHttpResponse<ListEvmTokenBalancesResponse> listEvmTokenBalances(
      ListEvmTokenBalancesNetwork network, String address, ListEvmTokenBalancesRequest request) {
    return listEvmTokenBalances(network,address,request,null);
  }

  /**
   * Lists the token balances of an EVM address on a given network. The balances include ERC-20 tokens and the native gas token (usually ETH). The response is paginated, and by default, returns 20 balances per page.
   * <p>Calls to the <code>base</code> and <code>base-sepolia</code> networks offer:</p>
   * <ul>
   * <li>99.5% &lt; 2 second freshness from chain tip for new tokens on an address.</li>
   * <li>99.5% &lt; 2 second freshness from chain tip for balance data.</li>
   * </ul>
   * <p>Calls to the <code>ethereum</code> network offer:</p>
   * <ul>
   * <li>99% &lt; 30 second freshness from chain tip for new tokens on an address.</li>
   * <li>99% &lt; 2 second freshness from chain tip for balance data.</li>
   * </ul>
   */
  public CdpClientHttpResponse<ListEvmTokenBalancesResponse> listEvmTokenBalances(
      ListEvmTokenBalancesNetwork network, String address, ListEvmTokenBalancesRequest request,
      RequestOptions requestOptions) {
    HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl()).newBuilder()

      .addPathSegments("v2/evm/token-balances")
      .addPathSegment(network.toString())
      .addPathSegment(address);if (request.getPageSize().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageSize", request.getPageSize().get(), false);
      }
      if (request.getPageToken().isPresent()) {
        QueryStringMapper.addQueryParameter(httpUrl, "pageToken", request.getPageToken().get(), false);
      }
      if (requestOptions != null) {
        requestOptions.getQueryParameters().forEach((_key, _value) -> {
          httpUrl.addQueryParameter(_key, _value);
        } );
      }
      Request.Builder _requestBuilder = new Request.Builder()
        .url(httpUrl.build())
        .method("GET", null)
        .tag(EndpointMetadata.class, new EndpointMetadata(Collections.emptyList(), Collections.emptyList()))
        .headers(Headers.of(clientOptions.headers(requestOptions)))
        .addHeader("Accept", "application/json");
      Request okhttpRequest = _requestBuilder.build();
      OkHttpClient client = clientOptions.httpClient();
      if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
        client = clientOptions.httpClientWithTimeout(requestOptions);
      }
      try (Response response = client.newCall(okhttpRequest).execute()) {
        ResponseBody responseBody = response.body();
        String responseBodyString = responseBody != null ? responseBody.string() : "{}";
        if (response.isSuccessful()) {
          return new CdpClientHttpResponse<>(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, ListEvmTokenBalancesResponse.class), response);
        }
        try {
          switch (response.code()) {
            case 400:throw new BadRequestError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 404:throw new NotFoundError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 500:throw new InternalServerError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 502:throw new BadGatewayError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
            case 503:throw new ServiceUnavailableError(ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Error.class), response);
          }
        }
        catch (JsonProcessingException ignored) {
          // unable to map error response, throwing generic error
        }
        Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
        throw new CdpClientApiException("Error with status code " + response.code(), response.code(), errorBody, response);
      }
      catch (JsonProcessingException e) {
        throw new CdpClientException("Failed to decode HTTP response", e);
      }
      catch (IOException e) {
        throw new CdpClientException("Network error executing HTTP request", e);
      }
    }
  }
