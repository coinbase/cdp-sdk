/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.types;

import com.coinbase.cdp.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.annotation.Nulls;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import java.util.Optional;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = Token.Builder.class
)
public final class Token {
  private final ListEvmTokenBalancesNetwork network;

  private final Optional<String> symbol;

  private final Optional<String> name;

  private final String contractAddress;

  private final Map<String, Object> additionalProperties;

  private Token(ListEvmTokenBalancesNetwork network, Optional<String> symbol, Optional<String> name,
      String contractAddress, Map<String, Object> additionalProperties) {
    this.network = network;
    this.symbol = symbol;
    this.name = name;
    this.contractAddress = contractAddress;
    this.additionalProperties = additionalProperties;
  }

  @JsonProperty("network")
  public ListEvmTokenBalancesNetwork getNetwork() {
    return network;
  }

  /**
   * @return The symbol of this token (ex: SOL, ETH, USDC).
   * The token symbol is not unique. It is possible for two different tokens to have the same symbol.
   * For native gas tokens, this symbol is defined via convention. As an example, for ETH on Ethereum mainnet, the symbol is &quot;ETH&quot;. For ERC-20 tokens, this symbol is defined via configuration. <code>symbol</code> will be the string returned by <code>function symbol() public view returns (string)</code> on the underlying token contract.
   * Not all tokens have a symbol, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#symbol">optional in the ERC-20 specification</a>. This field will only be populated when the token's underlying ERC-20 contract has a <code>symbol()</code> function.
   * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.
   */
  @JsonProperty("symbol")
  public Optional<String> getSymbol() {
    return symbol;
  }

  /**
   * @return The name of this token (ex: &quot;Solana&quot;, &quot;Ether&quot;, &quot;USD Coin&quot;).
   * The token name is not unique. It is possible for two different tokens to have the same name.
   * For native gas tokens, this name is defined via convention. As an example, for ETH on Ethereum mainnet, the name is &quot;Ether&quot;. For ERC-20 tokens, this name is defined via configuration. <code>name</code> will be the string returned by <code>function name() public view returns (string)</code> on the underlying token contract.
   * Not all tokens have a name, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#name">optional in the ERC-20 specification</a>. This field will only be populated when the token's underlying ERC-20 contract has a <code>name()</code> function.
   * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.
   */
  @JsonProperty("name")
  public Optional<String> getName() {
    return name;
  }

  /**
   * @return The contract address of the token.
   * For Ether, the contract address is <code>0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE</code> per <a href="https://github.com/ethereum/EIPs/blob/master/EIPS/eip-7528.md">EIP-7528</a>. For ERC-20 tokens, this is the contract address where the token is deployed.
   */
  @JsonProperty("contractAddress")
  public String getContractAddress() {
    return contractAddress;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof Token && equalTo((Token) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(Token other) {
    return network.equals(other.network) && symbol.equals(other.symbol) && name.equals(other.name) && contractAddress.equals(other.contractAddress);
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.network, this.symbol, this.name, this.contractAddress);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static NetworkStage builder() {
    return new Builder();
  }

  public interface NetworkStage {
    ContractAddressStage network(@NotNull ListEvmTokenBalancesNetwork network);

    Builder from(Token other);
  }

  public interface ContractAddressStage {
    /**
     * <p>The contract address of the token.
     * For Ether, the contract address is <code>0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE</code> per <a href="https://github.com/ethereum/EIPs/blob/master/EIPS/eip-7528.md">EIP-7528</a>. For ERC-20 tokens, this is the contract address where the token is deployed.</p>
     */
    _FinalStage contractAddress(@NotNull String contractAddress);
  }

  public interface _FinalStage {
    Token build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);

    /**
     * <p>The symbol of this token (ex: SOL, ETH, USDC).
     * The token symbol is not unique. It is possible for two different tokens to have the same symbol.
     * For native gas tokens, this symbol is defined via convention. As an example, for ETH on Ethereum mainnet, the symbol is &quot;ETH&quot;. For ERC-20 tokens, this symbol is defined via configuration. <code>symbol</code> will be the string returned by <code>function symbol() public view returns (string)</code> on the underlying token contract.
     * Not all tokens have a symbol, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#symbol">optional in the ERC-20 specification</a>. This field will only be populated when the token's underlying ERC-20 contract has a <code>symbol()</code> function.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</p>
     */
    _FinalStage symbol(Optional<String> symbol);

    _FinalStage symbol(String symbol);

    /**
     * <p>The name of this token (ex: &quot;Solana&quot;, &quot;Ether&quot;, &quot;USD Coin&quot;).
     * The token name is not unique. It is possible for two different tokens to have the same name.
     * For native gas tokens, this name is defined via convention. As an example, for ETH on Ethereum mainnet, the name is &quot;Ether&quot;. For ERC-20 tokens, this name is defined via configuration. <code>name</code> will be the string returned by <code>function name() public view returns (string)</code> on the underlying token contract.
     * Not all tokens have a name, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#name">optional in the ERC-20 specification</a>. This field will only be populated when the token's underlying ERC-20 contract has a <code>name()</code> function.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</p>
     */
    _FinalStage name(Optional<String> name);

    _FinalStage name(String name);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements NetworkStage, ContractAddressStage, _FinalStage {
    private ListEvmTokenBalancesNetwork network;

    private String contractAddress;

    private Optional<String> name = Optional.empty();

    private Optional<String> symbol = Optional.empty();

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(Token other) {
      network(other.getNetwork());
      symbol(other.getSymbol());
      name(other.getName());
      contractAddress(other.getContractAddress());
      return this;
    }

    @java.lang.Override
    @JsonSetter("network")
    public ContractAddressStage network(@NotNull ListEvmTokenBalancesNetwork network) {
      this.network = Objects.requireNonNull(network, "network must not be null");
      return this;
    }

    /**
     * <p>The contract address of the token.
     * For Ether, the contract address is <code>0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE</code> per <a href="https://github.com/ethereum/EIPs/blob/master/EIPS/eip-7528.md">EIP-7528</a>. For ERC-20 tokens, this is the contract address where the token is deployed.</p>
     * <p>The contract address of the token.
     * For Ether, the contract address is <code>0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE</code> per <a href="https://github.com/ethereum/EIPs/blob/master/EIPS/eip-7528.md">EIP-7528</a>. For ERC-20 tokens, this is the contract address where the token is deployed.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("contractAddress")
    public _FinalStage contractAddress(@NotNull String contractAddress) {
      this.contractAddress = Objects.requireNonNull(contractAddress, "contractAddress must not be null");
      return this;
    }

    /**
     * <p>The name of this token (ex: &quot;Solana&quot;, &quot;Ether&quot;, &quot;USD Coin&quot;).
     * The token name is not unique. It is possible for two different tokens to have the same name.
     * For native gas tokens, this name is defined via convention. As an example, for ETH on Ethereum mainnet, the name is &quot;Ether&quot;. For ERC-20 tokens, this name is defined via configuration. <code>name</code> will be the string returned by <code>function name() public view returns (string)</code> on the underlying token contract.
     * Not all tokens have a name, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#name">optional in the ERC-20 specification</a>. This field will only be populated when the token's underlying ERC-20 contract has a <code>name()</code> function.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage name(String name) {
      this.name = Optional.ofNullable(name);
      return this;
    }

    /**
     * <p>The name of this token (ex: &quot;Solana&quot;, &quot;Ether&quot;, &quot;USD Coin&quot;).
     * The token name is not unique. It is possible for two different tokens to have the same name.
     * For native gas tokens, this name is defined via convention. As an example, for ETH on Ethereum mainnet, the name is &quot;Ether&quot;. For ERC-20 tokens, this name is defined via configuration. <code>name</code> will be the string returned by <code>function name() public view returns (string)</code> on the underlying token contract.
     * Not all tokens have a name, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#name">optional in the ERC-20 specification</a>. This field will only be populated when the token's underlying ERC-20 contract has a <code>name()</code> function.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "name",
        nulls = Nulls.SKIP
    )
    public _FinalStage name(Optional<String> name) {
      this.name = name;
      return this;
    }

    /**
     * <p>The symbol of this token (ex: SOL, ETH, USDC).
     * The token symbol is not unique. It is possible for two different tokens to have the same symbol.
     * For native gas tokens, this symbol is defined via convention. As an example, for ETH on Ethereum mainnet, the symbol is &quot;ETH&quot;. For ERC-20 tokens, this symbol is defined via configuration. <code>symbol</code> will be the string returned by <code>function symbol() public view returns (string)</code> on the underlying token contract.
     * Not all tokens have a symbol, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#symbol">optional in the ERC-20 specification</a>. This field will only be populated when the token's underlying ERC-20 contract has a <code>symbol()</code> function.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    public _FinalStage symbol(String symbol) {
      this.symbol = Optional.ofNullable(symbol);
      return this;
    }

    /**
     * <p>The symbol of this token (ex: SOL, ETH, USDC).
     * The token symbol is not unique. It is possible for two different tokens to have the same symbol.
     * For native gas tokens, this symbol is defined via convention. As an example, for ETH on Ethereum mainnet, the symbol is &quot;ETH&quot;. For ERC-20 tokens, this symbol is defined via configuration. <code>symbol</code> will be the string returned by <code>function symbol() public view returns (string)</code> on the underlying token contract.
     * Not all tokens have a symbol, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#symbol">optional in the ERC-20 specification</a>. This field will only be populated when the token's underlying ERC-20 contract has a <code>symbol()</code> function.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</p>
     */
    @java.lang.Override
    @JsonSetter(
        value = "symbol",
        nulls = Nulls.SKIP
    )
    public _FinalStage symbol(Optional<String> symbol) {
      this.symbol = symbol;
      return this;
    }

    @java.lang.Override
    public Token build() {
      return new Token(network, symbol, name, contractAddress, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
