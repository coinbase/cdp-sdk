/**
 * This file was auto-generated by Fern from our API Definition.
 */

package com.coinbase.cdp.types;

import com.coinbase.cdp.core.ObjectMappers;
import com.fasterxml.jackson.annotation.JsonAnyGetter;
import com.fasterxml.jackson.annotation.JsonAnySetter;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonSetter;
import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import java.lang.Object;
import java.lang.String;
import java.util.HashMap;
import java.util.Map;
import java.util.Objects;
import org.jetbrains.annotations.NotNull;

@JsonInclude(JsonInclude.Include.NON_ABSENT)
@JsonDeserialize(
    builder = TokenAmount.Builder.class
)
public final class TokenAmount {
  private final String amount;

  private final long decimals;

  private final Map<String, Object> additionalProperties;

  private TokenAmount(String amount, long decimals, Map<String, Object> additionalProperties) {
    this.amount = amount;
    this.decimals = decimals;
    this.additionalProperties = additionalProperties;
  }

  /**
   * @return The amount is denominated in the smallest indivisible unit of the token. For ETH, the smallest indivisible unit is Wei (10^-18 ETH). For ERC-20s, the smallest unit is the unit returned from <code>function totalSupply() public view returns (uint256)</code>.
   */
  @JsonProperty("amount")
  public String getAmount() {
    return amount;
  }

  /**
   * @return 'decimals' is the exponential value N that satisfies the equation <code>amount * 10^-N = standard_denomination</code>. The standard denomination is the most commonly used denomination for the token.
   * <ul>
   * <li>In the case of the native gas token, <code>decimals</code> is defined via convention. As an example, for ETH of Ethereum mainnet, the standard denomination is 10^-18 the smallest denomination (Wei). As such, for ETH on Ethereum mainnet, <code>decimals</code> is 18. - In the case of ERC-20 tokens, <code>decimals</code> is defined via configuration. <code>decimals</code> will be the number returned by <code>function decimals() public view returns (uint8)</code> on the underlying token contract.
   * Not all tokens have a <code>decimals</code> field, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#decimals">optional in the ERC-20 specification</a>. This field will be left empty if the underlying token contract doesn't implement <code>decimals</code>.
   * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</li>
   * </ul>
   */
  @JsonProperty("decimals")
  public long getDecimals() {
    return decimals;
  }

  @java.lang.Override
  public boolean equals(Object other) {
    if (this == other) return true;
    return other instanceof TokenAmount && equalTo((TokenAmount) other);
  }

  @JsonAnyGetter
  public Map<String, Object> getAdditionalProperties() {
    return this.additionalProperties;
  }

  private boolean equalTo(TokenAmount other) {
    return amount.equals(other.amount) && decimals == other.decimals;
  }

  @java.lang.Override
  public int hashCode() {
    return Objects.hash(this.amount, this.decimals);
  }

  @java.lang.Override
  public String toString() {
    return ObjectMappers.stringify(this);
  }

  public static AmountStage builder() {
    return new Builder();
  }

  public interface AmountStage {
    /**
     * <p>The amount is denominated in the smallest indivisible unit of the token. For ETH, the smallest indivisible unit is Wei (10^-18 ETH). For ERC-20s, the smallest unit is the unit returned from <code>function totalSupply() public view returns (uint256)</code>.</p>
     */
    DecimalsStage amount(@NotNull String amount);

    Builder from(TokenAmount other);
  }

  public interface DecimalsStage {
    /**
     * <p>'decimals' is the exponential value N that satisfies the equation <code>amount * 10^-N = standard_denomination</code>. The standard denomination is the most commonly used denomination for the token.</p>
     * <ul>
     * <li>In the case of the native gas token, <code>decimals</code> is defined via convention. As an example, for ETH of Ethereum mainnet, the standard denomination is 10^-18 the smallest denomination (Wei). As such, for ETH on Ethereum mainnet, <code>decimals</code> is 18. - In the case of ERC-20 tokens, <code>decimals</code> is defined via configuration. <code>decimals</code> will be the number returned by <code>function decimals() public view returns (uint8)</code> on the underlying token contract.
     * Not all tokens have a <code>decimals</code> field, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#decimals">optional in the ERC-20 specification</a>. This field will be left empty if the underlying token contract doesn't implement <code>decimals</code>.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</li>
     * </ul>
     */
    _FinalStage decimals(long decimals);
  }

  public interface _FinalStage {
    TokenAmount build();

    _FinalStage additionalProperty(String key, Object value);

    _FinalStage additionalProperties(Map<String, Object> additionalProperties);
  }

  @JsonIgnoreProperties(
      ignoreUnknown = true
  )
  public static final class Builder implements AmountStage, DecimalsStage, _FinalStage {
    private String amount;

    private long decimals;

    @JsonAnySetter
    private Map<String, Object> additionalProperties = new HashMap<>();

    private Builder() {
    }

    @java.lang.Override
    public Builder from(TokenAmount other) {
      amount(other.getAmount());
      decimals(other.getDecimals());
      return this;
    }

    /**
     * <p>The amount is denominated in the smallest indivisible unit of the token. For ETH, the smallest indivisible unit is Wei (10^-18 ETH). For ERC-20s, the smallest unit is the unit returned from <code>function totalSupply() public view returns (uint256)</code>.</p>
     * <p>The amount is denominated in the smallest indivisible unit of the token. For ETH, the smallest indivisible unit is Wei (10^-18 ETH). For ERC-20s, the smallest unit is the unit returned from <code>function totalSupply() public view returns (uint256)</code>.</p>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("amount")
    public DecimalsStage amount(@NotNull String amount) {
      this.amount = Objects.requireNonNull(amount, "amount must not be null");
      return this;
    }

    /**
     * <p>'decimals' is the exponential value N that satisfies the equation <code>amount * 10^-N = standard_denomination</code>. The standard denomination is the most commonly used denomination for the token.</p>
     * <ul>
     * <li>In the case of the native gas token, <code>decimals</code> is defined via convention. As an example, for ETH of Ethereum mainnet, the standard denomination is 10^-18 the smallest denomination (Wei). As such, for ETH on Ethereum mainnet, <code>decimals</code> is 18. - In the case of ERC-20 tokens, <code>decimals</code> is defined via configuration. <code>decimals</code> will be the number returned by <code>function decimals() public view returns (uint8)</code> on the underlying token contract.
     * Not all tokens have a <code>decimals</code> field, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#decimals">optional in the ERC-20 specification</a>. This field will be left empty if the underlying token contract doesn't implement <code>decimals</code>.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</li>
     * </ul>
     * <p>'decimals' is the exponential value N that satisfies the equation <code>amount * 10^-N = standard_denomination</code>. The standard denomination is the most commonly used denomination for the token.</p>
     * <ul>
     * <li>In the case of the native gas token, <code>decimals</code> is defined via convention. As an example, for ETH of Ethereum mainnet, the standard denomination is 10^-18 the smallest denomination (Wei). As such, for ETH on Ethereum mainnet, <code>decimals</code> is 18. - In the case of ERC-20 tokens, <code>decimals</code> is defined via configuration. <code>decimals</code> will be the number returned by <code>function decimals() public view returns (uint8)</code> on the underlying token contract.
     * Not all tokens have a <code>decimals</code> field, as this function is <a href="https://eips.ethereum.org/EIPS/eip-20#decimals">optional in the ERC-20 specification</a>. This field will be left empty if the underlying token contract doesn't implement <code>decimals</code>.
     * Further, this endpoint will only populate this value for a small subset of whitelisted ERC-20 tokens at this time. We intend to improve coverage in the future.</li>
     * </ul>
     * @return Reference to {@code this} so that method calls can be chained together.
     */
    @java.lang.Override
    @JsonSetter("decimals")
    public _FinalStage decimals(long decimals) {
      this.decimals = decimals;
      return this;
    }

    @java.lang.Override
    public TokenAmount build() {
      return new TokenAmount(amount, decimals, additionalProperties);
    }

    @java.lang.Override
    public Builder additionalProperty(String key, Object value) {
      this.additionalProperties.put(key, value);
      return this;
    }

    @java.lang.Override
    public Builder additionalProperties(Map<String, Object> additionalProperties) {
      this.additionalProperties.putAll(additionalProperties);
      return this;
    }
  }
}
