---
"@coinbase/cdp-sdk": minor
---

Bumped the `@x402/core`, `@x402/evm`, `@x402/extensions`, and `@x402/svm` peer dependencies to `^2.24.0`, and expanded `CdpX402Client`/`createX402Server` scheme coverage: the `upto` scheme is now supported on Solana (client and server), and `CdpX402Client` now registers the `authCapture` scheme by default on Base, alongside `exact` and `upto`.
