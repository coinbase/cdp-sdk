---
"@coinbase/cdp-sdk": minor
---

Bump the `@x402/core`, `@x402/evm`, `@x402/extensions`, and `@x402/svm` peer dependencies to `^2.19.0`, improve `CdpX402Client` network registration, and add `batch-settlement` scheme support for clients.
