/**
 * Generated by orval v7.6.0 🍺
 * Do not edit manually.
 * Coinbase Developer Platform APIs
 * The Coinbase Developer Platform APIs - leading the world's transition onchain.
 * OpenAPI spec version: 2.0.0
 */
import type {
  RegisterWalletSecretBody,
  UpdateWalletSecretBody,
  WalletSecretMetadata,
} from "../coinbaseDeveloperPlatformAPIs.schemas.js";

import { cdpApiClient } from "../../cdpApiClient.js";

type SecondParameter<T extends (...args: never) => unknown> = Parameters<T>[1];

/**
 * Registers a Wallet Secret for a CDP Project. This secret is configured in the CDP Portal, and is used to authenticate requests to the EVM and Solana Account APIs.


Wallet Secrets should be generated in the browser using the following JavaScript code. The private key should be returned to the developer, and the public key should be what is passed to this API.

```javascript
window.crypto.subtle.generateKey(
  {
    name: "ECDSA",
    namedCurve: "P-256", // secp256r1
  },
  true, // Allow key export
  ["sign", "verify"] // Key usages
).then(keyPair => {
  return Promise.all([
    window.crypto.subtle.exportKey("pkcs8", keyPair.privateKey),
    window.crypto.subtle.exportKey("spki", keyPair.publicKey)
  ]);
}).then(([privateKeyBuffer, publicKeyBuffer]) => {
  const privateKeyBytes = new Uint8Array(privateKeyBuffer);
  const publicKeyBytes = new Uint8Array(publicKeyBuffer);

  // Safe way to convert large Uint8Arrays to strings
  const privateKeyBase64 = btoa(
    Array.from(privateKeyBytes)
      .map(byte => String.fromCharCode(byte))
      .join('')
  );

  const publicKeyBase64 = btoa(
    Array.from(publicKeyBytes)
      .map(byte => String.fromCharCode(byte))
      .join('')
  );

  console.log(privateKeyBase64);
  console.log(publicKeyBase64);

  return { privateKeyBase64, publicKeyBase64 };
});
```

This API is available only to the CDP Portal, and should not be exposed externally.
 * @summary Register a Wallet Secret
 */
export const registerWalletSecret = (
  registerWalletSecretBody: RegisterWalletSecretBody,
  options?: SecondParameter<typeof cdpApiClient>,
) => {
  return cdpApiClient<void>(
    {
      url: `/v2/wallet/secrets`,
      method: "POST",
      headers: { "Content-Type": "application/json" },
      data: registerWalletSecretBody,
    },
    options,
  );
};
/**
 * Gets the currently active Wallet Secret in the CDP project. This API is available only to the CDP Portal, and should not be exposed externally.
 * @summary Get the currently active Wallet Secret
 */
export const getWalletSecretMetadata = (options?: SecondParameter<typeof cdpApiClient>) => {
  return cdpApiClient<WalletSecretMetadata>({ url: `/v2/wallet/secrets`, method: "GET" }, options);
};
/**
 * Updates the Wallet Secret for a CDP Project. This updated secret is configured in the CDP Portal, and is used to authenticate requests to the EVM and Solana Account APIs. The existing Wallet Secret in the project will be unregistered and can no longer be used. 
This API is available only to the CDP Portal, and should not be exposed externally.
 * @summary Update Wallet Secret
 */
export const updateWalletSecret = (
  updateWalletSecretBody: UpdateWalletSecretBody,
  options?: SecondParameter<typeof cdpApiClient>,
) => {
  return cdpApiClient<void>(
    {
      url: `/v2/wallet/secrets`,
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      data: updateWalletSecretBody,
    },
    options,
  );
};
export type RegisterWalletSecretResult = NonNullable<
  Awaited<ReturnType<typeof registerWalletSecret>>
>;
export type GetWalletSecretMetadataResult = NonNullable<
  Awaited<ReturnType<typeof getWalletSecretMetadata>>
>;
export type UpdateWalletSecretResult = NonNullable<Awaited<ReturnType<typeof updateWalletSecret>>>;
