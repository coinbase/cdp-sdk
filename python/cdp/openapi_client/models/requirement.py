# coding: utf-8

"""
    Coinbase Developer Platform APIs

    The Coinbase Developer Platform APIs - leading the world's transition onchain.

    The version of the OpenAPI document: 2.0.0
    Contact: cdp@coinbase.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from cdp.openapi_client.models.capability_name import CapabilityName
from cdp.openapi_client.models.requirement_status import RequirementStatus
from cdp.openapi_client.models.tax_form import TaxForm
from cdp.openapi_client.models.terms_of_service import TermsOfService
from typing import Optional, Set
from typing_extensions import Self

class Requirement(BaseModel):
    """
    A single requirement that a customer must submit to enable capabilities. Requirements are only shown for requested capabilities. 
    """ # noqa: E501
    status: RequirementStatus
    deadline: Optional[datetime] = Field(default=None, description="Optional deadline by which this requirement must be satisfied.  For the `tos` requirement, when present, `deadline` is the earliest deadline among the unaccepted required Terms of Service versions. ")
    impact: Optional[List[CapabilityName]] = Field(default=None, description="List of capabilities affected by this requirement, sorted alphabetically. Only present for `due`, `pending`, or `rejected` statuses. ")
    tos_versions: Optional[List[TermsOfService]] = Field(default=None, description="Required Terms of Service versions the Customer has not yet accepted, with the metadata needed to render an acceptance UI (each entry carries a stable `versionId`, the BCP 47 `languages` the version is published in, and a language-agnostic `url`). This field appears only under the requirement key `tos`; do not infer meaning from `tosVersions` on other requirement keys. Only populated for the `tos` requirement; omitted on every other requirement key. Submit each `versionId` back via `tosAcceptances[].versionId` on `Update Customer` (or `Create Customer`) together with `language` and `acceptedAt` to clear the requirement. ", alias="tosVersions")
    tax_forms: Optional[List[TaxForm]] = Field(default=None, description="Required tax attestation forms the Customer has not yet completed. This field appears only under the requirement key `taxAttestation`. Submit each `form` back via `taxAttestations[].form` on `Update Customer` (or `Create Customer`) together with the other fields required for that form to clear the requirement. ", alias="taxForms")
    __properties: ClassVar[List[str]] = ["status", "deadline", "impact", "tosVersions", "taxForms"]

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of Requirement from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in tos_versions (list)
        _items = []
        if self.tos_versions:
            for _item_tos_versions in self.tos_versions:
                if _item_tos_versions:
                    _items.append(_item_tos_versions.to_dict())
            _dict['tosVersions'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of Requirement from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "status": obj.get("status"),
            "deadline": obj.get("deadline"),
            "impact": obj.get("impact"),
            "tosVersions": [TermsOfService.from_dict(_item) for _item in obj["tosVersions"]] if obj.get("tosVersions") is not None else None,
            "taxForms": obj.get("taxForms")
        })
        return _obj


