# coding: utf-8

"""
    Coinbase Developer Platform APIs

    The Coinbase Developer Platform APIs - leading the world's transition onchain.

    The version of the OpenAPI document: 2.0.0
    Contact: cdp@coinbase.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from cdp.openapi_client.models.x402_batch_settlement_channel_config import X402BatchSettlementChannelConfig
from cdp.openapi_client.models.x402_batch_settlement_claim import X402BatchSettlementClaim
from cdp.openapi_client.models.x402_batch_settlement_voucher import X402BatchSettlementVoucher
from typing import Optional, Set
from typing_extensions import Self

class X402BatchSettlementRefundPayload(BaseModel):
    """
    A cooperative refund request. The client emits the minimal shape (just `channelConfig` and `voucher`, with an optional `amount`). A mediating server enriches the payload with `amount`, `refundNonce`, and `claims` before forwarding to the facilitator. Authorizer signatures are optional — the facilitator auto-signs when absent. Field presence determines which shape was sent; the facilitator dispatches accordingly.
    """ # noqa: E501
    type: StrictStr = Field(description="The payload-type discriminator. Must be `\"refund\"` for both the client-emitted and server-enriched shape.")
    channel_config: X402BatchSettlementChannelConfig = Field(alias="channelConfig")
    voucher: X402BatchSettlementVoucher
    amount: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The refund amount in atomic units of `channelConfig.token`. Optional in the client-emitted shape (defaults to the full remaining channel balance). Required when the payload is enriched by a mediating server.")
    refund_nonce: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The on-chain refund nonce for replay protection (uint256 as decimal string). Only present on the server-enriched shape.", alias="refundNonce")
    claims: Optional[List[X402BatchSettlementClaim]] = Field(default=None, description="Voucher claims to include atomically with the refund. Only present on the server-enriched shape.")
    refund_authorizer_signature: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Optional EIP-712 signature from the receiver authorizer over the refund. When omitted, the facilitator auto-signs.", alias="refundAuthorizerSignature")
    claim_authorizer_signature: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="Optional EIP-712 signature from the receiver authorizer over the included claims. When omitted, the facilitator auto-signs.", alias="claimAuthorizerSignature")
    __properties: ClassVar[List[str]] = ["type", "channelConfig", "voucher", "amount", "refundNonce", "claims", "refundAuthorizerSignature", "claimAuthorizerSignature"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['refund']):
            raise ValueError("must be one of enum values ('refund')")
        return value

    @field_validator('amount')
    def amount_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('refund_nonce')
    def refund_nonce_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[0-9]+$", value):
            raise ValueError(r"must validate the regular expression /^[0-9]+$/")
        return value

    @field_validator('refund_authorizer_signature')
    def refund_authorizer_signature_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^0x[0-9a-fA-F]+$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]+$/")
        return value

    @field_validator('claim_authorizer_signature')
    def claim_authorizer_signature_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^0x[0-9a-fA-F]+$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]+$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of X402BatchSettlementRefundPayload from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of channel_config
        if self.channel_config:
            _dict['channelConfig'] = self.channel_config.to_dict()
        # override the default output from pydantic by calling `to_dict()` of voucher
        if self.voucher:
            _dict['voucher'] = self.voucher.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in claims (list)
        _items = []
        if self.claims:
            for _item_claims in self.claims:
                if _item_claims:
                    _items.append(_item_claims.to_dict())
            _dict['claims'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of X402BatchSettlementRefundPayload from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "channelConfig": X402BatchSettlementChannelConfig.from_dict(obj["channelConfig"]) if obj.get("channelConfig") is not None else None,
            "voucher": X402BatchSettlementVoucher.from_dict(obj["voucher"]) if obj.get("voucher") is not None else None,
            "amount": obj.get("amount"),
            "refundNonce": obj.get("refundNonce"),
            "claims": [X402BatchSettlementClaim.from_dict(_item) for _item in obj["claims"]] if obj.get("claims") is not None else None,
            "refundAuthorizerSignature": obj.get("refundAuthorizerSignature"),
            "claimAuthorizerSignature": obj.get("claimAuthorizerSignature")
        })
        return _obj


