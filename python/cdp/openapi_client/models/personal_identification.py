# coding: utf-8

"""
    Coinbase Developer Platform APIs

    The Coinbase Developer Platform APIs - leading the world's transition onchain.

    The version of the OpenAPI document: 2.0.0
    Contact: cdp@coinbase.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class PersonalIdentification(BaseModel):
    """
    A government-issued personal identifier, paired with its type and issuing country so downstream Travel Rule reporting can classify the identifier correctly. Field shapes mirror IVMS-101 `NationalIdentification` (the format Travel Rule reporting ultimately marshals to) — most notably the 35-character maximum on `value`.
    """ # noqa: E501
    type: StrictStr = Field(description="The kind of government-issued identifier carried in `value`. Values map 1:1 to IVMS-101 `NationalIdentifierTypeCode` so downstream Travel Rule reporting can serialize without ambiguity.")
    value: Annotated[str, Field(min_length=1, strict=True, max_length=35)] = Field(description="The identifier as issued by the relevant authority. Length matches the IVMS-101 `NationalIdentifier` 35-character cap. Letters, digits, and common separators (hyphens, spaces, dots) are accepted; the exact character set depends on `type` and `countryOfIssue`.")
    country_of_issue: Optional[Annotated[str, Field(min_length=2, strict=True, max_length=2)]] = Field(default=None, description="ISO 3166-1 alpha-2 country code of the authority that issued the identifier. Required when the identifier format depends on the issuing jurisdiction (e.g. a \"national ID\" means different things in DE vs. BR).", alias="countryOfIssue")
    __properties: ClassVar[List[str]] = ["type", "value", "countryOfIssue"]

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['social_security_number', 'tax_id', 'passport_number', 'national_id_card', 'drivers_license']):
            raise ValueError("must be one of enum values ('social_security_number', 'tax_id', 'passport_number', 'national_id_card', 'drivers_license')")
        return value

    @field_validator('country_of_issue')
    def country_of_issue_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^[A-Z]{2}$", value):
            raise ValueError(r"must validate the regular expression /^[A-Z]{2}$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of PersonalIdentification from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of PersonalIdentification from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "type": obj.get("type"),
            "value": obj.get("value"),
            "countryOfIssue": obj.get("countryOfIssue")
        })
        return _obj


