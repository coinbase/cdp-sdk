# coding: utf-8

"""
    Coinbase Developer Platform APIs

    The Coinbase Developer Platform APIs - leading the world's transition onchain.

    The version of the OpenAPI document: 2.0.0
    Contact: cdp@coinbase.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class X402BatchSettlementChannelConfig(BaseModel):
    """
    Immutable configuration for an x402 batch-settlement payment channel. The EIP-712 hash of this struct produces the `channelId` used by all batch-settlement payloads.
    """ # noqa: E501
    payer: Annotated[str, Field(strict=True)] = Field(description="The 0x-prefixed, checksum EVM address of the payer (channel funder).")
    payer_authorizer: Annotated[str, Field(strict=True)] = Field(description="The 0x-prefixed, checksum EVM address authorized to sign vouchers on behalf of the payer.", alias="payerAuthorizer")
    receiver: Annotated[str, Field(strict=True)] = Field(description="The 0x-prefixed, checksum EVM address of the receiver (resource server / merchant).")
    receiver_authorizer: Annotated[str, Field(strict=True)] = Field(description="The 0x-prefixed, checksum EVM address authorized to sign claim batches on behalf of the receiver (typically the facilitator).", alias="receiverAuthorizer")
    token: Annotated[str, Field(strict=True)] = Field(description="The 0x-prefixed, checksum EVM address of the ERC-20 payment token.")
    withdraw_delay: Annotated[int, Field(le=2592000, strict=True, ge=900)] = Field(description="The non-cooperative withdraw delay in seconds. Must be between 900 (15 minutes) and 2,592,000 (30 days).", alias="withdrawDelay")
    salt: Annotated[str, Field(strict=True)] = Field(description="A 32-byte salt used to differentiate channels between the same payer/receiver pair.")
    __properties: ClassVar[List[str]] = ["payer", "payerAuthorizer", "receiver", "receiverAuthorizer", "token", "withdrawDelay", "salt"]

    @field_validator('payer')
    def payer_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[0-9a-fA-F]{40}$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]{40}$/")
        return value

    @field_validator('payer_authorizer')
    def payer_authorizer_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[0-9a-fA-F]{40}$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]{40}$/")
        return value

    @field_validator('receiver')
    def receiver_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[0-9a-fA-F]{40}$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]{40}$/")
        return value

    @field_validator('receiver_authorizer')
    def receiver_authorizer_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[0-9a-fA-F]{40}$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]{40}$/")
        return value

    @field_validator('token')
    def token_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[0-9a-fA-F]{40}$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]{40}$/")
        return value

    @field_validator('salt')
    def salt_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not re.match(r"^0x[0-9a-fA-F]{64}$", value):
            raise ValueError(r"must validate the regular expression /^0x[0-9a-fA-F]{64}$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of X402BatchSettlementChannelConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of X402BatchSettlementChannelConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "payer": obj.get("payer"),
            "payerAuthorizer": obj.get("payerAuthorizer"),
            "receiver": obj.get("receiver"),
            "receiverAuthorizer": obj.get("receiverAuthorizer"),
            "token": obj.get("token"),
            "withdrawDelay": obj.get("withdrawDelay"),
            "salt": obj.get("salt")
        })
        return _obj


