# coding: utf-8

"""
    Coinbase Developer Platform APIs

    The Coinbase Developer Platform APIs - leading the world's transition onchain.

    The version of the OpenAPI document: 2.0.0
    Contact: cdp@coinbase.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing import Optional, Set
from typing_extensions import Self

class X402ValidateCheck(BaseModel):
    """
    The result of a single preflight check performed against the x402 endpoint.
    """ # noqa: E501
    check: StrictStr = Field(description="The name of the preflight check. New values may be added in future versions; clients should handle unknown values gracefully.")
    passed: StrictBool = Field(description="Whether the check passed.")
    detail: StrictStr = Field(description="A human-readable explanation of the check result.")
    expected: Optional[StrictStr] = Field(default=None, description="The value the check expected, when applicable.")
    actual: Optional[StrictStr] = Field(default=None, description="The value the check observed, when applicable.")
    severity: StrictStr = Field(description="Whether this check is a hard indexing requirement or a quality recommendation. A `required` check affects validity when it fails. An `advisory` check (for example, a missing output schema) is a suggestion to improve discoverability and does not make the endpoint invalid.")
    __properties: ClassVar[List[str]] = ["check", "passed", "detail", "expected", "actual", "severity"]

    @field_validator('check')
    def check_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['reachable', 'returns_402', 'has_bazaar_extension', 'parse']):
            raise ValueError("must be one of enum values ('reachable', 'returns_402', 'has_bazaar_extension', 'parse')")
        return value

    @field_validator('severity')
    def severity_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['required', 'advisory']):
            raise ValueError("must be one of enum values ('required', 'advisory')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of X402ValidateCheck from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of X402ValidateCheck from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "check": obj.get("check"),
            "passed": obj.get("passed"),
            "detail": obj.get("detail"),
            "expected": obj.get("expected"),
            "actual": obj.get("actual"),
            "severity": obj.get("severity")
        })
        return _obj


