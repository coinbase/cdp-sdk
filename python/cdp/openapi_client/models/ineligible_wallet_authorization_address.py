# coding: utf-8

"""
    Coinbase Developer Platform APIs

    The Coinbase Developer Platform APIs - leading the world's transition onchain.

    The version of the OpenAPI document: 2.0.0
    Contact: cdp@coinbase.com
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from cdp.openapi_client.models.wallet_authorization_funds_requirement import WalletAuthorizationFundsRequirement
from typing import Optional, Set
from typing_extensions import Self

class IneligibleWalletAuthorizationAddress(BaseModel):
    """
    A requested payer wallet address that has no eligible authorization option for this payment session, along with a machine- and human-readable reason.
    """ # noqa: E501
    address: Annotated[str, Field(min_length=1, strict=True, max_length=128)] = Field(description="The requested payer wallet address that has no eligible authorization option.")
    code: StrictStr = Field(description="A machine-readable code indicating why this address has no eligible authorization option. The enum is closed — any value the server returns must be listed below. Adding a new code is a deliberate, coordinated API change; clients receiving an undocumented value should treat it as a server violating the spec.")
    message: StrictStr = Field(description="A human-readable, English-language description of why this address has no eligible authorization option. Suitable for surfacing in product UIs — does not contain personally identifiable information or internal infrastructure details. Clients that need localized strings should dispatch on `code` and provide their own translations.")
    funds_required: Optional[List[WalletAuthorizationFundsRequirement]] = Field(default=None, description="The funding options for this address, one entry per (network, asset) combination the payer could fund to become eligible. Only present when `code` is `insufficient_funds`, and honors the request's `network` and `asset` filters. May be an empty array.", alias="fundsRequired")
    __properties: ClassVar[List[str]] = ["address", "code", "message", "fundsRequired"]

    @field_validator('code')
    def code_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['insufficient_funds', 'superseded_by_preferred_option']):
            raise ValueError("must be one of enum values ('insufficient_funds', 'superseded_by_preferred_option')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IneligibleWalletAuthorizationAddress from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in funds_required (list)
        _items = []
        if self.funds_required:
            for _item_funds_required in self.funds_required:
                if _item_funds_required:
                    _items.append(_item_funds_required.to_dict())
            _dict['fundsRequired'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IneligibleWalletAuthorizationAddress from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "address": obj.get("address"),
            "code": obj.get("code"),
            "message": obj.get("message"),
            "fundsRequired": [WalletAuthorizationFundsRequirement.from_dict(_item) for _item in obj["fundsRequired"]] if obj.get("fundsRequired") is not None else None
        })
        return _obj


