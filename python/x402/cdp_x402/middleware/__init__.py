"""Web framework middleware for cdp-x402.

Submodules:
- :mod:`cdp_x402.middleware.flask`   — Flask sync middleware.
- :mod:`cdp_x402.middleware.fastapi` — FastAPI async middleware.

Both build on the framework-agnostic primitives in :mod:`cdp_x402.middleware._common`.
"""
